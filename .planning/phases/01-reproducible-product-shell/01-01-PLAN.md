---
phase: 01-reproducible-product-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - CMakePresets.json
  - .gitignore
  - cmake/CPM.cmake
  - cmake/Dependencies.cmake
  - cmake/WebBundle.cmake
  - scripts/check-environment.ps1
  - scripts/test-quick.ps1
  - scripts/test-all.ps1
  - plugin/CMakeLists.txt
  - plugin/include/LumaScope/PluginProcessor.h
  - plugin/include/LumaScope/PluginEditor.h
  - plugin/include/LumaScope/HostBridge.h
  - plugin/include/LumaScope/WebResources.h
  - plugin/source/PluginProcessor.cpp
  - plugin/source/PluginEditor.cpp
  - plugin/source/HostBridge.cpp
  - plugin/source/WebResources.cpp
  - ui/package.json
  - ui/package-lock.json
  - ui/tsconfig.json
  - ui/vite.config.ts
  - ui/vitest.config.ts
  - ui/index.html
  - ui/src/main.tsx
  - ui/src/App.tsx
  - ui/src/bridge/protocol.ts
  - ui/src/bridge/BridgeProvider.tsx
  - ui/src/test/setup.ts
  - tests/native/CMakeLists.txt
  - tests/native/HostBridgeTests.cpp
  - tests/native/WebResourcesTests.cpp
  - tests/fixtures/bridge/ui-ready-v1.json
  - tests/fixtures/bridge/host-info-v1.json
  - tests/fixtures/bridge/unsupported-version.json
  - tests/fixtures/bridge/malformed.json
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - UI-01
  - UI-03
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: Built products identify as LumaScope by Signal Foundry Audio with JUCE codes SgFd and LmSc."
    - "D-05 D-06 D-07 D-08: The first supported configure/build path proves Windows 10 x64, VS2019 16.11, and Evergreen WebView2 prerequisites without silently selecting VS2022."
    - "D-13 D-14 D-15 D-17: A clean checkout has a PowerShell preflight, npm lockfile, embedded-default build, and explicit VS2019/Ninja presets."
    - "Launching the standalone Debug artifact loads an embedded React shell and changes from connecting to ready after a protocol-v1 native/web handshake."
    - "The VST3 and Standalone artifacts are produced from the same LumaScope shared-code target."
  artifacts:
    - path: "CMakePresets.json"
      provides: "Explicit VS2019 and Ninja configure/build presets"
      contains: "Visual Studio 16 2019"
    - path: "plugin/CMakeLists.txt"
      provides: "JUCE VST3/Standalone target with WebView2 and embedded assets"
      contains: "juce_add_plugin(LumaScope"
    - path: "plugin/source/HostBridge.cpp"
      provides: "Protocol-v1 native handshake implementation"
      min_lines: 30
    - path: "ui/src/bridge/BridgeProvider.tsx"
      provides: "Typed browser bridge adapter and connection state"
      min_lines: 35
    - path: "scripts/check-environment.ps1"
      provides: "Machine-readable prerequisite validation"
      contains: "param("
  key_links:
    - from: "ui/src/bridge/BridgeProvider.tsx"
      to: "plugin/source/HostBridge.cpp"
      via: "ui.ready/host.info protocol-v1 fixture contract"
      pattern: "ui\\.ready|host\\.info"
    - from: "plugin/source/PluginEditor.cpp"
      to: "plugin/source/WebResources.cpp"
      via: "WebBrowserComponent ResourceProvider"
      pattern: "withResourceProvider"
    - from: "cmake/WebBundle.cmake"
      to: "plugin/CMakeLists.txt"
      via: "generated web archive and juce_add_binary_data dependency"
      pattern: "WebBundle|BinaryData"
---

<objective>
Create the thinnest working LumaScope product slice: reproducible Windows targets, embedded React assets, and a real protocol-v1 native/web handshake visible in the standalone shell.

Purpose: Prove the riskiest toolchain and integration seams before visual polish or later product features grow around them.
Output: Build/test scaffold, VST3 and standalone binaries, embedded minimal React shell, path-safe resource provider, typed handshake fixtures, and quick/full validation commands.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reproducible-product-shell/01-CONTEXT.md
@.planning/phases/01-reproducible-product-shell/01-RESEARCH.md
@.planning/phases/01-reproducible-product-shell/01-VALIDATION.md
@.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md
@.planning/phases/01-reproducible-product-shell/SKELETON.md
</context>

<threat_model>
## Threat Model

| ID | Asset/Boundary | Threat | Severity | Required Mitigation | Verification |
|----|----------------|--------|----------|---------------------|--------------|
| T-01-01 | Dependency/build inputs | Floating or substituted JUCE/WebView/npm dependencies make builds non-reproducible | high | Pin JUCE `8.0.14`, CPM bootstrap, WebView2 SDK, and npm lockfile; print selected generator/compiler/version | Preflight JSON and configure logs assert exact selections |
| T-01-02 | Embedded resource provider | `..`, query strings, unknown MIME types, or missing entries expose/serve unintended bytes | high | Normalize relative paths, reject traversal/absolute paths, strip query/fragment, allowlist MIME types, return `nullopt` on unknown/missing | Native fixture tests cover valid and hostile paths |
| T-01-03 | Native/web bridge | Malformed or unsupported payload reaches native callbacks | high | Protocol version `1`, object/type/size validation, stable error envelope, no arbitrary script evaluation | Native and Vitest fixture tests reject malformed/unsupported messages |

No high-severity threat may remain unmitigated when this plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prove the locked Windows toolchain and scaffold JUCE targets</name>
  <files>CMakeLists.txt, CMakePresets.json, .gitignore, cmake/CPM.cmake, cmake/Dependencies.cmake, scripts/check-environment.ps1, plugin/CMakeLists.txt, plugin/include/LumaScope/PluginProcessor.h, plugin/include/LumaScope/PluginEditor.h, plugin/source/PluginProcessor.cpp, plugin/source/PluginEditor.cpp, tests/native/CMakeLists.txt</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-CONTEXT.md, .planning/phases/01-reproducible-product-shell/01-RESEARCH.md, .planning/phases/01-reproducible-product-shell/SKELETON.md, CMakeLists.txt, CMakePresets.json, scripts/check-environment.ps1, plugin/CMakeLists.txt</read_first>
  <action>Create a CMake 3.22+ C++20 project named `LumaScope` with JUCE `8.0.14`, CPM.cmake `v0.42.3`, and Microsoft.Web.WebView2 `1.0.4022.49` pinned exactly; use a repository-local/cacheable dependency directory excluded by `.gitignore`. Add explicit `vs2019-debug`, `vs2019-release`, `ninja-debug`, and `ninja-release` configure/build presets plus matching CTest presets; the VS presets must name generator `Visual Studio 16 2019` and architecture `x64`, never rely on latest-generator selection. Implement `scripts/check-environment.ps1` with normal human output and `-Json`; report Windows product/build/architecture, VS installation/version/C++ workload, CMake, Node, npm, Ninja, and Evergreen WebView2 runtime. Fail with nonzero exit and actionable text when a required prerequisite is missing. Add `juce_add_plugin(LumaScope)` with company `Signal Foundry Audio`, codes `SgFd`/`LmSc`, formats `VST3 Standalone`, `NEEDS_WEB_BROWSER TRUE`, `NEEDS_WEBVIEW2 TRUE`, no MIDI, and product `LumaScope`. Add minimal pass-through processor/editor source and a native CTest target. Run the preflight and immediately configure/build with VS2019; if JUCE 8.0.14 or WebView2 1.0.4022.49 fails due to unsupported compiler/toolset, stop this plan and report the blocker rather than changing versions or toolchain.</action>
  <verify>pwsh -NoProfile -File scripts/check-environment.ps1 -Json; cmake --preset vs2019-debug; cmake --build --preset vs2019-debug --target LumaScope_Standalone LumaScope_VST3 LumaScopeNativeTests</verify>
  <acceptance_criteria>
    - `CMakeLists.txt` contains `project(LumaScope` and `CMAKE_CXX_STANDARD 20`.
    - `CMakePresets.json` contains generator `Visual Studio 16 2019`, architecture `x64`, and named VS2019/Ninja Debug/Release presets.
    - `plugin/CMakeLists.txt` contains `COMPANY_NAME "Signal Foundry Audio"`, `PLUGIN_MANUFACTURER_CODE SgFd`, `PLUGIN_CODE LmSc`, `FORMATS VST3 Standalone`, `NEEDS_WEB_BROWSER TRUE`, and `NEEDS_WEBVIEW2 TRUE`.
    - `scripts/check-environment.ps1 -Json` exits 0 on the baseline machine and emits parseable JSON containing Windows build, VS version, CMake, Node, npm, Ninja, and WebView2 fields.
    - VS2019 Debug build exits 0 and produces LumaScope standalone/VST3 targets plus `LumaScopeNativeTests`.
  </acceptance_criteria>
  <done>The locked VS2019/JUCE 8.0.14 baseline is proven and both product target types compile from explicit presets.</done>
</task>

<task type="auto">
  <name>Task 2: Build and embed the minimal typed React shell</name>
  <files>cmake/WebBundle.cmake, plugin/CMakeLists.txt, plugin/include/LumaScope/WebResources.h, plugin/source/WebResources.cpp, ui/package.json, ui/package-lock.json, ui/tsconfig.json, ui/vite.config.ts, ui/vitest.config.ts, ui/index.html, ui/src/main.tsx, ui/src/App.tsx, ui/src/bridge/protocol.ts, ui/src/bridge/BridgeProvider.tsx, ui/src/test/setup.ts, tests/native/WebResourcesTests.cpp, tests/fixtures/web/index.html</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-RESEARCH.md, .planning/phases/01-reproducible-product-shell/01-UI-SPEC.md, plugin/CMakeLists.txt, plugin/source/PluginEditor.cpp, ui/package.json, ui/vite.config.ts, ui/src/App.tsx, cmake/WebBundle.cmake</read_first>
  <action>Initialize `ui/` with exact versions React/React DOM `19.2.7`, TypeScript `6.0.3`, Vite `8.0.16`, `@vitejs/plugin-react` `6.0.2`, Material UI/icons `9.1.1`, Emotion React `11.14.0`, Emotion Styled `11.14.1`, Vitest `4.1.9`, jsdom `29.1.1`, React Testing Library `16.3.2`, and jest-dom `6.9.1`, plus a committed `package-lock.json`. Add scripts `dev`, `build`, `test`, `test:run`, and `check:bundle`; no script may require a global package. Configure Vite `base: './'`, clean `dist/`, and no external font/CDN resources. Create the minimal honest shell with LumaScope name, `Spectrum analyzer` subtitle, analyzer-stage region, and connecting status; defer visual polish to Plan 02. Implement `cmake/WebBundle.cmake` to run `npm ci` only when lock/package inputs change, run `npm run build`, create a deterministic zip rooted at the Vite output, embed that generated zip through `juce_add_binary_data`, and make shared code depend on it. Implement `WebResources` to open the embedded zip, map `/` to `index.html`, normalize and reject unsafe paths, allowlist MIME types for html/js/css/json/svg/png/ico/woff2, and return `nullopt` for missing/unknown resources. Add native resource tests and a frontend bundle check that fails if `dist/` references `http://`, `https://`, Google Fonts, or a CDN.</action>
  <verify>npm --prefix ui ci; npm --prefix ui run test:run; npm --prefix ui run build; npm --prefix ui run check:bundle; cmake --build --preset vs2019-debug --target LumaScope_Standalone LumaScope_VST3 LumaScopeNativeTests; ctest --preset vs2019-debug --output-on-failure</verify>
  <acceptance_criteria>
    - `ui/package-lock.json` exists and `npm --prefix ui ci` exits 0.
    - `ui/vite.config.ts` sets relative base and deterministic clean build behavior.
    - Built `dist/index.html` uses relative/local assets and `check:bundle` reports no remote URL/font/CDN references.
    - `plugin/source/WebResources.cpp` rejects `..`, absolute paths, unknown extensions, and missing entries while serving the fixture's HTML/JS/CSS with correct MIME types.
    - Rebuilding after changing `ui/src/App.tsx` regenerates the embedded archive before linking both native targets.
  </acceptance_criteria>
  <done>The same local React bundle is deterministically built, archived, embedded, and path-safely served to both LumaScope formats.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the protocol-v1 native/web handshake</name>
  <files>plugin/include/LumaScope/HostBridge.h, plugin/source/HostBridge.cpp, plugin/include/LumaScope/PluginEditor.h, plugin/source/PluginEditor.cpp, ui/src/bridge/protocol.ts, ui/src/bridge/BridgeProvider.tsx, ui/src/App.tsx, tests/native/HostBridgeTests.cpp, tests/fixtures/bridge/ui-ready-v1.json, tests/fixtures/bridge/host-info-v1.json, tests/fixtures/bridge/unsupported-version.json, tests/fixtures/bridge/malformed.json, scripts/test-quick.ps1, scripts/test-all.ps1</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-CONTEXT.md, .planning/phases/01-reproducible-product-shell/01-RESEARCH.md, .planning/phases/01-reproducible-product-shell/01-VALIDATION.md, plugin/source/PluginEditor.cpp, plugin/source/HostBridge.cpp, ui/src/bridge/protocol.ts, ui/src/bridge/BridgeProvider.tsx, tests/fixtures/bridge/ui-ready-v1.json</read_first>
  <action>Define protocol version `1`, event `ui.ready`, response/event `host.info`, and error envelope `{ code, message, protocolVersion }` in native constants and TypeScript types backed by the same committed JSON fixtures. `ui.ready` must contain `{ protocolVersion: 1 }`. `host.info` must contain `{ protocolVersion: 1, productName: 'LumaScope', companyName: 'Signal Foundry Audio', productVersion, hostMode: 'VST3'|'Standalone', uiSource: 'embedded'|'vite' }`. Configure `WebBrowserComponent` explicitly with WebView2 backend, native integration, resource provider root, initialization data, and event listener/native function(s); do not use arbitrary string-built JavaScript evaluation for the handshake. Validate object shape, protocol number, and bounded string fields before responding. React's `BridgeProvider` owns connecting/ready/error state and renders `Embedded UI · Bridge ready` after valid host info, while mock transport supports browser tests. Add a Debug-only standalone smoke hook controlled by environment variable `LUMASCOPE_SMOKE_RESULT_FILE`: after handshake success or terminal error, write bounded JSON `{ status, protocolVersion, uiSource, errorCode? }` to that path and exit cleanly; compile the hook out of non-Debug builds. Implement `scripts/test-quick.ps1` for non-watch Vitest plus warm native CTest and `scripts/test-all.ps1` for UI build/tests, VS2019 build of both formats/tests, and CTest.</action>
  <verify>pwsh -NoProfile -File scripts/test-quick.ps1; pwsh -NoProfile -File scripts/test-all.ps1</verify>
  <acceptance_criteria>
    - All four bridge fixture files exist and both native and Vitest suites consume them.
    - Valid protocol-v1 fixture transitions React from connecting to ready and renders `LumaScope`, `Standalone` or `VST3`, and `Embedded UI · Bridge ready`.
    - Unsupported and malformed fixtures produce stable typed error codes and never call the success path.
    - `PluginEditor.cpp` explicitly requests WebView2, enables native integration, installs the resource provider and handshake listener, and navigates to `getResourceProviderRoot()` in embedded mode.
    - Debug standalone with `LUMASCOPE_SMOKE_RESULT_FILE` writes handshake JSON containing `status: ready`, protocol `1`, and source `embedded`; Release sources contain no active smoke hook.
    - `scripts/test-quick.ps1` and `scripts/test-all.ps1` exit 0 without watch mode; full script builds both formats.
  </acceptance_criteria>
  <done>Both LumaScope products contain a real, tested native/web protocol-v1 path and the embedded standalone visibly reports bridge readiness.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/check-environment.ps1 -Json` confirms the locked machine/toolchain without hidden substitution.
- [ ] `npm --prefix ui run test:run` and `npm --prefix ui run build` pass.
- [ ] `cmake --preset vs2019-debug` and builds for Standalone, VST3, and native tests pass.
- [ ] `ctest --preset vs2019-debug --output-on-failure` passes.
- [ ] `scripts/test-all.ps1` exits 0 and embedded standalone can reach `Embedded UI · Bridge ready`.
</verification>

<success_criteria>
- All tasks completed and all high-severity threat mitigations are tested.
- A clean checkout can produce both LumaScope formats from explicit VS2019/CMake/npm inputs.
- Embedded assets are authoritative, offline, and rebuilt when frontend sources change.
- The protocol-v1 handshake is validated on native and browser sides and visibly drives the shell state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reproducible-product-shell/01-01-SUMMARY.md`
</output>
