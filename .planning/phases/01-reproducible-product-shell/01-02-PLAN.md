---
phase: 01-reproducible-product-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - ui/src/app/theme.ts
  - ui/src/app/AppShell.tsx
  - ui/src/components/BrandHeader.tsx
  - ui/src/components/AnalyzerStage.tsx
  - ui/src/components/BridgeStatus.tsx
  - ui/src/components/StatusFooter.tsx
  - ui/src/App.tsx
  - ui/src/styles/spectral-motif.css
  - ui/src/test/AppShell.test.tsx
  - ui/src/test/BridgeStates.test.tsx
  - ui/src/test/Accessibility.test.tsx
  - ui/src/test/BundleContract.test.ts
  - plugin/source/PluginEditor.cpp
autonomous: false
requirements:
  - BUILD-03
  - UI-01
  - UI-03
user_setup: []
must_haves:
  truths:
    - "D-09 D-10 D-11 D-12: LumaScope presents a dark instrument-panel shell with restrained cyan/lime accents, compact header, dominant honest analyzer placeholder, and quiet footer without later-phase controls or fake spectrum data."
    - "At 960x600 and 560x360, connecting, ready, and typed error states remain readable without essential overlap or horizontal scrolling."
    - "The production UI contains no network font, CDN, analytics, or remote image dependency."
    - "Keyboard focus, reduced motion, status text, and semantic regions meet the approved UI-SPEC contract."
  artifacts:
    - path: "ui/src/app/theme.ts"
      provides: "Central semantic LumaScope Material UI theme"
      contains: "#0B0F12"
    - path: "ui/src/app/AppShell.tsx"
      provides: "Three-row product shell"
      min_lines: 40
    - path: "ui/src/components/AnalyzerStage.tsx"
      provides: "Stable Phase 2 renderer mount and honest Phase 1 state"
      contains: "Spectrum display"
    - path: "ui/src/test/Accessibility.test.tsx"
      provides: "Keyboard/semantic/reduced-motion assertions"
      min_lines: 25
  key_links:
    - from: "ui/src/app/AppShell.tsx"
      to: "ui/src/bridge/BridgeProvider.tsx"
      via: "bridge state selects status and recovery presentation"
      pattern: "useBridge|BridgeStatus"
    - from: "ui/src/App.tsx"
      to: "ui/src/app/theme.ts"
      via: "ThemeProvider and CssBaseline"
      pattern: "ThemeProvider|CssBaseline"
---

<objective>
Turn the proven walking skeleton into the approved LumaScope visual and accessibility contract without adding Phase 2/3/6 product behavior.

Purpose: Lock the durable visual frame and state UX that every later phase extends.
Output: Semantic MUI theme, shell components, honest bridge states, responsive/accessibility tests, and user-approved embedded presentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reproducible-product-shell/01-CONTEXT.md
@.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md
@.planning/phases/01-reproducible-product-shell/01-VALIDATION.md
@.planning/phases/01-reproducible-product-shell/01-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Asset/Boundary | Threat | Severity | Required Mitigation | Verification |
|----|----------------|--------|----------|---------------------|--------------|
| T-01-05 | Packaged UI privacy/offline behavior | Theme/icons/fonts silently fetch remote content | high | System fonts, bundled MUI/icons/assets, bundle forbidden-URL scan | Bundle contract test and production dist scan |
| T-01-06 | Diagnostic content | Host-provided strings become unsafe markup or unbounded layout content | medium | Render as React text, bound/truncate values, never use `dangerouslySetInnerHTML` | Tests with markup and long diagnostic fixture |

No high-severity threat may remain unmitigated when this plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement the semantic LumaScope theme and shell hierarchy</name>
  <files>ui/src/app/theme.ts, ui/src/app/AppShell.tsx, ui/src/components/BrandHeader.tsx, ui/src/components/AnalyzerStage.tsx, ui/src/components/StatusFooter.tsx, ui/src/styles/spectral-motif.css, ui/src/App.tsx, ui/src/test/AppShell.test.tsx</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md, .planning/phases/01-reproducible-product-shell/01-CONTEXT.md, ui/src/App.tsx, ui/src/bridge/BridgeProvider.tsx, ui/src/app/theme.ts, ui/src/app/AppShell.tsx</read_first>
  <action>Implement one MUI theme module using Segoe UI Variable/Segoe UI/system fallback, CSS variables where supported, approved spacing/type/radius/elevation tokens, canvas `#0B0F12`, surface `#121A20`, raised `#172129`, border `#24323A`, text `#F2F7F8`/`#91A4AE`, cyan `#53D8FB`, lime `#B7F36B`, warning `#FFC857`, and destructive `#FF6B78`. Wrap the app in ThemeProvider/CssBaseline. Build a viewport-filling grid with rows `52px minmax(0,1fr) 32px`, default 960x600 native sizing, 16px stage inset (12px below 640px), compact header, single elevated stage, and quiet footer. AnalyzerStage must be a region labeled `Spectrum display`, show `Analyzer surface ready` and the approved body only when ready, and expose a stable child/mount boundary for Phase 2. Implement the spectral motif as decorative CSS/SVG with low-opacity localized glow, no fabricated bins/Hz/dB values, no knobs/settings/source/license controls, and reduced-motion support.</action>
  <verify>npm --prefix ui run test:run -- AppShell.test.tsx; npm --prefix ui run build</verify>
  <acceptance_criteria>
    - Theme module contains every approved hex value and exports one theme consumed through ThemeProvider/CssBaseline.
    - AppShell uses the exact 52px/1fr/32px grid and stage inset breakpoints from UI-SPEC.
    - AnalyzerStage has accessible name `Spectrum display`, contains no fake numeric spectrum values, and no source/settings/license control labels.
    - No component outside `theme.ts` contains the approved palette hex literals except focused visual test fixtures.
    - Reduced-motion CSS disables any decorative drift animation.
  </acceptance_criteria>
  <done>The LumaScope shell has the approved visual hierarchy, tokens, scope fence, and Phase 2 mount boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed connection/error UX and accessibility verification</name>
  <files>ui/src/components/BridgeStatus.tsx, ui/src/components/StatusFooter.tsx, ui/src/app/AppShell.tsx, ui/src/test/BridgeStates.test.tsx, ui/src/test/Accessibility.test.tsx, ui/src/test/BundleContract.test.ts, plugin/source/PluginEditor.cpp</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md, ui/src/bridge/protocol.ts, ui/src/bridge/BridgeProvider.tsx, ui/src/app/AppShell.tsx, ui/src/components/BridgeStatus.tsx, plugin/source/PluginEditor.cpp</read_first>
  <action>Map only typed bridge states to approved copy: `Connecting to audio engine…`, `Embedded UI · Bridge ready`, `Vite development · Bridge ready`, development-server unavailable, protocol mismatch with rebuild guidance, and runtime error with WebView2 guidance. Add `Retry connection` only where transport retry is meaningful and `Copy diagnostics` for non-retry details. Use semantic buttons, a polite non-spamming live region, visible 2px cyan keyboard focus with 2px offset, minimum 32x32 compact targets/36px recovery buttons, text+icon status (never color alone), and no `dangerouslySetInnerHTML`. Hide decorative icons, bound untrusted diagnostic strings, and keep long errors from breaking minimum layout. Add RTL/Vitest coverage for connecting/ready/all errors, VST3/Standalone labels, embedded/vite source, keyboard traversal, semantic region/button names, reduced-motion styling, long/markup diagnostic strings, and bundle forbidden URLs. Update native editor background/fallback colors to match the approved dominant/surface palette where React is unavailable.</action>
  <verify>npm --prefix ui run test:run; npm --prefix ui run build; npm --prefix ui run check:bundle; pwsh -NoProfile -File scripts/test-quick.ps1</verify>
  <acceptance_criteria>
    - Every copy string in the UI-SPEC state table is asserted by a deterministic component test.
    - Status changes use a polite live region and tests find recovery actions by accessible role/name.
    - Source contains no `dangerouslySetInnerHTML`, Google Fonts, CDN URL, analytics call, or remote image URL.
    - Markup-like diagnostics render as literal text and long diagnostics do not create horizontal document overflow in the test harness.
    - Native editor fallback uses colors matching `#0B0F12`/`#121A20` and does not depend on React to explain a WebView failure.
  </acceptance_criteria>
  <done>All approved bridge states are actionable, accessible, offline-safe, and protected from unsafe diagnostic rendering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the embedded LumaScope visual shell</name>
  <files>ui/src/app/AppShell.tsx, ui/src/components/AnalyzerStage.tsx, plugin/source/PluginEditor.cpp</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md, ui/src/app/AppShell.tsx, ui/src/components/AnalyzerStage.tsx, plugin/source/PluginEditor.cpp</read_first>
  <action>Pause execution after the automated build/tests and present the real embedded standalone for human visual and accessibility verification at the approved default/minimum sizes and Windows scaling.</action>
  <verify>User returns `approved`; any described issue returns execution to the relevant UI task before this checkpoint can pass.</verify>
  <acceptance_criteria>
    - User explicitly approves the embedded shell after checking identity, hierarchy, scope fence, bridge-ready state, keyboard focus, resize, and scaling.
  </acceptance_criteria>
  <what-built>LumaScope Phase 1 visual shell in the standalone WebView, including connecting, ready, and diagnostic states.</what-built>
  <how-to-verify>Launch the VS2019 Debug standalone in embedded mode. Verify: 960x600 dark instrument-panel presentation; LumaScope/Signal Foundry identity; restrained cyan/lime accents; compact header, one dominant stage, quiet footer; no fake spectrum data or future controls; bridge-ready state; keyboard-visible recovery focus. Resize to 560x360 and test Windows 150% scaling. Type `approved` if the shell matches the contract, or describe concrete visual/accessibility issues.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User approval is recorded with no unresolved visual or accessibility issue.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Full Vitest suite, production UI build, and bundle contract pass.
- [ ] `scripts/test-quick.ps1` passes after native fallback styling changes.
- [ ] Human verifies embedded shell at default/minimum size and scaled Windows display.
- [ ] No later-phase controls or fake spectrum data are present.
</verification>

<success_criteria>
- All UI-SPEC visual, copy, typography, spacing, accessibility, motion, asset, and registry contracts are implemented.
- Connecting/ready/error states are driven only by typed bridge data.
- User approves the real embedded standalone presentation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reproducible-product-shell/01-02-SUMMARY.md`
</output>
