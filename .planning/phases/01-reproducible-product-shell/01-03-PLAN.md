---
phase: 01-reproducible-product-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - CMakePresets.json
  - plugin/CMakeLists.txt
  - plugin/include/LumaScope/PluginEditor.h
  - plugin/source/PluginEditor.cpp
  - plugin/source/HostBridge.cpp
  - ui/src/bridge/BridgeProvider.tsx
  - ui/src/components/BridgeStatus.tsx
  - scripts/check-environment.ps1
  - scripts/bootstrap.ps1
  - scripts/test-web-modes.ps1
  - scripts/verify-project.ps1
  - scripts/test-all.ps1
  - .codex/config.toml
  - AGENTS.md
  - README.md
  - docs/development.md
  - docs/bridge-protocol.md
  - docs/troubleshooting.md
autonomous: false
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-04
  - BUILD-05
  - UI-03
user_setup: []
must_haves:
  truths:
    - "D-16: Vite hot reload is opt-in, fixed to loopback, visibly labeled, protocol-compatible with embedded mode, and reversible without source edits."
    - "A new developer can run one PowerShell bootstrap/check entry point, then explicit documented CMake/npm commands, without hidden dashboard/global dependency steps."
    - "One project `.codex/config.toml` configures Context7 at https://mcp.context7.com/mcp; AGENTS.md and development docs record all three approved library IDs."
    - "Missing Vite server, protocol mismatch, packaged resource failure, and WebView2/runtime failure each produce actionable diagnostics rather than a blank editor."
    - "The final verification script proves both native formats, frontend/native tests, embedded bundle provenance, and forbidden URL/secret checks."
  artifacts:
    - path: ".codex/config.toml"
      provides: "Trusted-project Context7 MCP connection"
      contains: "[mcp_servers.context7]"
    - path: "scripts/bootstrap.ps1"
      provides: "Documented reproducible setup entry point"
      contains: "check-environment.ps1"
    - path: "scripts/test-web-modes.ps1"
      provides: "Embedded and Vite mode smoke orchestration"
      min_lines: 35
    - path: "docs/bridge-protocol.md"
      provides: "Protocol v1 contract and extension rules"
      contains: "ui.ready"
    - path: "docs/troubleshooting.md"
      provides: "Actionable toolchain/WebView2/assets/dev-server diagnostics"
      contains: "WebView2"
  key_links:
    - from: "CMakePresets.json"
      to: "plugin/source/PluginEditor.cpp"
      via: "LUMASCOPE_WEBVIEW_DEV_SERVER cache variable"
      pattern: "LUMASCOPE_WEBVIEW_DEV_SERVER"
    - from: ".codex/config.toml"
      to: "AGENTS.md"
      via: "Context7 server plus required library-ID guidance"
      pattern: "context7"
    - from: "scripts/bootstrap.ps1"
      to: "scripts/check-environment.ps1"
      via: "preflight before dependency/configure operations"
      pattern: "check-environment"
---

<objective>
Harden the LumaScope shell for daily development and handoff: explicit Vite mode, native failure diagnostics, one setup path, complete verification, and correct project-scoped Context7 configuration.

Purpose: Ensure the shell works outside the original author's warm machine and that embedded/reload modes cannot drift or fail silently.
Output: Safe development-server selection, diagnostic/fallback paths, bootstrap and smoke scripts, Context7 MCP config/guidance, and complete developer/troubleshooting/protocol documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reproducible-product-shell/01-CONTEXT.md
@.planning/phases/01-reproducible-product-shell/01-RESEARCH.md
@.planning/phases/01-reproducible-product-shell/01-VALIDATION.md
@.planning/phases/01-reproducible-product-shell/01-UI-SPEC.md
@.planning/phases/01-reproducible-product-shell/01-01-SUMMARY.md
@.planning/phases/01-reproducible-product-shell/01-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| ID | Asset/Boundary | Threat | Severity | Required Mitigation | Verification |
|----|----------------|--------|----------|---------------------|--------------|
| T-01-04 | Dev-server/native integration | Arbitrary remote origin receives JUCE native capabilities | high | Accept only empty or canonical `http://127.0.0.1:<fixed-port>` value in Debug; reject hostnames, non-loopback, paths, credentials, and release-mode dev URL | CMake/config tests and hostile URL cases |
| T-01-07 | Diagnostics/logs/config | Environment output or docs leak tokens/paths/secrets | high | No secret values in committed config/output; optional Context7 key remains user environment; verification scans common token/private-key patterns | `verify-project.ps1` and git grep assertions |
| T-01-08 | Build mode confusion | Release silently points to Vite or embedded artifact is stale | high | Release hard-disables dev URL, host.info reports UI source/build marker, embedded archive is a target dependency | Build-mode tests and standalone smoke |

No high-severity threat may remain unmitigated when this plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit Vite mode and native-owned diagnostics</name>
  <files>CMakePresets.json, plugin/CMakeLists.txt, plugin/include/LumaScope/PluginEditor.h, plugin/source/PluginEditor.cpp, plugin/source/HostBridge.cpp, ui/src/bridge/BridgeProvider.tsx, ui/src/components/BridgeStatus.tsx, scripts/test-web-modes.ps1</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-CONTEXT.md, .planning/phases/01-reproducible-product-shell/01-RESEARCH.md, .planning/phases/01-reproducible-product-shell/01-UI-SPEC.md, CMakePresets.json, plugin/CMakeLists.txt, plugin/source/PluginEditor.cpp, plugin/source/HostBridge.cpp, ui/src/bridge/BridgeProvider.tsx, scripts/test-web-modes.ps1</read_first>
  <action>Add CMake cache variable `LUMASCOPE_WEBVIEW_DEV_SERVER` defaulting to empty. Add an explicit Debug-only preset/value for canonical `http://127.0.0.1:5173`; reject any non-empty release value at configure time. Native validation accepts only scheme `http`, host `127.0.0.1`, fixed port `5173`, no credentials/query/fragment, and root path. Embedded mode always navigates to JUCE's resource-provider root; Vite mode uses the validated URL and allowed origin while keeping the same protocol-v1 handshake. Include `uiSource` and a build marker in host info. Implement native-owned fallback content/panel for WebView2 creation/runtime failure, embedded resource navigation failure, unavailable Vite server, and handshake timeout; React owns protocol mismatch and runtime errors only after its bundle has loaded. Extend the Debug-only `LUMASCOPE_SMOKE_RESULT_FILE` hook so `scripts/test-web-modes.ps1` can assert actual handshake/failure JSON. The script must start Vite as a hidden/background child, wait for HTTP readiness, launch the standalone smoke hook in Vite mode, assert `{ status: 'ready', uiSource: 'vite' }`, clean up the child in `finally`, then run embedded mode and assert `{ status: 'ready', uiSource: 'embedded' }`; it must also run an unavailable-server case and assert the native error code. Include hostile URL/configure cases without requiring external network.</action>
  <verify>npm --prefix ui run test:run; pwsh -NoProfile -File scripts/test-web-modes.ps1; cmake --preset vs2019-release; cmake --build --preset vs2019-release --target LumaScope_Standalone LumaScope_VST3</verify>
  <acceptance_criteria>
    - Empty dev-server option selects embedded root; exact `http://127.0.0.1:5173` selects Vite only in Debug.
    - Release configure fails for any non-empty `LUMASCOPE_WEBVIEW_DEV_SERVER`.
    - Tests reject `localhost`, remote hosts, HTTPS, alternate ports, credentials, path/query/fragment, and malformed URLs.
    - Vite and embedded smoke paths both produce Debug smoke JSON with `status: ready`, protocol `1`, and the correct `uiSource`.
    - Unavailable Vite server produces a stable native smoke error code and actionable fallback; simulated WebView2/resource/handshake failures show approved native or React diagnostics instead of a blank editor.
  </acceptance_criteria>
  <done>Development hot reload is explicit and safe, embedded mode remains authoritative, and failures are diagnosable even when React cannot start.</done>
</task>

<task type="auto">
  <name>Task 2: Complete reproducible setup, Context7 guidance, and project verification</name>
  <files>scripts/check-environment.ps1, scripts/bootstrap.ps1, scripts/verify-project.ps1, scripts/test-all.ps1, .codex/config.toml, AGENTS.md, README.md, docs/development.md, docs/bridge-protocol.md, docs/troubleshooting.md</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-CONTEXT.md, .planning/phases/01-reproducible-product-shell/01-RESEARCH.md, .planning/phases/01-reproducible-product-shell/01-VALIDATION.md, .planning/phases/01-reproducible-product-shell/SKELETON.md, scripts/check-environment.ps1, scripts/test-all.ps1, AGENTS.md, README.md, .codex/config.toml</read_first>
  <action>Create `.codex/config.toml` containing exactly one `[mcp_servers.context7]` streamable HTTP server with `url = "https://mcp.context7.com/mcp"`, `enabled = true`, and `required = false`; do not commit API keys or create per-library servers. Update AGENTS.md with a concise rule to query Context7 for library/API setup and the IDs `/websites/juce_master`, `/janwilczek/juce-webview-tutorial`, and `/websites/mui_material-ui`. Implement `scripts/bootstrap.ps1` to run preflight first, then npm `ci`, CMake configure, and selected build with explicit parameters (`-Preset`, `-Configuration` or equivalent); default to VS2019 Debug and never install system software silently. Update `check-environment.ps1` to explain missing VS developer environment/Ninja/WebView2 SDK/runtime separately. Implement `verify-project.ps1` to assert required artifacts/config strings, exactly one Context7 server, all three IDs in guidance, no tutorial identity (`WolfSound`, `JuceWebViewPlugin`, `JWVT`, `WFSD`), no real account/license/key/private-key patterns, and no release localhost dependency. Make `test-all.ps1` call UI test/build/bundle check, native configure/build for VST3/Standalone/tests, CTest, and project verification with strict error propagation. Write README quickstart and development/troubleshooting/protocol docs with exact embedded/Vite commands, dependency cache behavior, protocol payloads/error codes, WebView2 recovery, and VS2019 blocker policy.</action>
  <verify>pwsh -NoProfile -File scripts/bootstrap.ps1 -Preset vs2019-debug; pwsh -NoProfile -File scripts/test-all.ps1; pwsh -NoProfile -File scripts/verify-project.ps1</verify>
  <acceptance_criteria>
    - `.codex/config.toml` contains exactly one MCP table, `[mcp_servers.context7]`, with URL `https://mcp.context7.com/mcp`, and contains no token/key value.
    - AGENTS.md and `docs/development.md` contain all three exact Context7 library IDs.
    - README documents clean checkout commands for preflight, bootstrap/build, tests, embedded launch, and Vite launch.
    - `docs/bridge-protocol.md` lists exact protocol-v1 `ui.ready`, `host.info`, and error envelope fields plus extension/versioning rules.
    - `verify-project.ps1` exits nonzero when seeded with a tutorial identity, extra Context7 server, remote asset URL, private key marker, or release dev-server value; it exits 0 on the repository.
    - `test-all.ps1` exits 0 and proves UI tests/build, both native formats, CTest, and project verification.
  </acceptance_criteria>
  <done>A new developer has a reproducible setup/test path and Codex has the correct project-scoped Context7 server plus library routing guidance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the complete embedded and Vite developer workflow</name>
  <files>scripts/bootstrap.ps1, scripts/test-web-modes.ps1, .codex/config.toml, AGENTS.md, docs/development.md, docs/troubleshooting.md</files>
  <read_first>.planning/phases/01-reproducible-product-shell/01-CONTEXT.md, scripts/bootstrap.ps1, scripts/test-web-modes.ps1, .codex/config.toml, AGENTS.md, docs/development.md, docs/troubleshooting.md</read_first>
  <action>Pause after automated verification and ask the user to exercise the documented fresh-terminal bootstrap, embedded mode, Vite hot reload, unavailable-server state, native fallback, and Context7 configuration.</action>
  <verify>User returns `approved`; any failed step returns execution to Task 1 or Task 2 for correction before re-presenting the checkpoint.</verify>
  <acceptance_criteria>
    - User explicitly approves every listed setup, embedded, Vite, failure-diagnostic, and Context7 verification step.
  </acceptance_criteria>
  <what-built>Complete Phase 1 LumaScope shell in embedded and Vite modes, with native fallbacks, reproducible setup, and project Context7 guidance.</what-built>
  <how-to-verify>From a fresh terminal, run the documented preflight/bootstrap. Launch standalone embedded mode and verify `Embedded UI · Bridge ready`; start the documented Vite task, launch the dev preset, edit a visible subtitle, and verify hot reload plus `Vite development · Bridge ready`. Stop Vite and verify the actionable unavailable message. Invoke the documented WebView/runtime diagnostic hook and verify the native fallback. Confirm `.codex/config.toml` lists one Context7 server and AGENTS.md names all three library IDs. Type `approved` or report the failing step and observed output.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User approval is recorded with no unresolved setup, mode, diagnostic, or MCP configuration issue.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Bootstrap succeeds from a fresh terminal without hidden global project dependencies.
- [ ] Embedded and Vite mode smoke checks report correct source and identical protocol version.
- [ ] Release mode cannot be configured with a dev URL.
- [ ] Full test/build/project verification script exits 0.
- [ ] Human approves end-to-end embedded, hot-reload, unavailable-server, and native-fallback behavior.
</verification>

<success_criteria>
- All Phase 1 requirements BUILD-01 through BUILD-05, UI-01, and UI-03 have executable proof.
- All high-severity dev-origin, secret/config, and build-mode threats are blocked by tests or configuration gates.
- Project guidance contains one valid Context7 MCP connection and all three requested library IDs.
- Another developer can follow the repository alone to build, test, and diagnose LumaScope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reproducible-product-shell/01-03-SUMMARY.md`
</output>
