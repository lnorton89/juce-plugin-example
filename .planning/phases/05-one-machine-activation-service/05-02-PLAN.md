---
phase: 05-one-machine-activation-service
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - worker/src/routes.ts
  - worker/src/api/activationRoutes.ts
  - worker/src/db/repository.ts
  - worker/src/db/schema.ts
  - worker/src/db/migrations/0002_activation_policy.sql
  - worker/src/signing/entitlement.ts
  - tests/worker/activation-routes.test.ts
  - tests/worker/repository.test.ts
  - tests/worker/setup.ts
  - docs/activation-api.md
autonomous: true
requirements:
  - CLOUD-04
  - CLOUD-05
  - CLOUD-06
  - CLOUD-07
  - CLOUD-10
requirements_addressed:
  - CLOUD-04
  - CLOUD-05
  - CLOUD-06
  - CLOUD-07
  - CLOUD-10
user_setup: []
must_haves:
  truths:
    - "D-06: Different-machine activation is rejected with activation_limit_reached while another active machine exists."
    - "D-07: Same-machine activation is idempotent success and returns a fresh signed entitlement envelope."
    - "D-08: Deactivate requires licenseKey plus matching machineId and only deactivates that active machine."
    - "D-09: Validate refreshes an existing active activation only and returns activation_not_found when missing/deactivated."
    - "Concurrent activation requests cannot create more than one active machine for a one-seat license."
  artifacts:
    - path: "worker/src/api/activationRoutes.ts"
      provides: "Activate, validate, and deactivate route handlers"
      contains: "handleActivate"
    - path: "worker/src/db/repository.ts"
      provides: "Concurrency-safe activation repository operations"
      contains: "activateMachine"
    - path: "tests/worker/activation-routes.test.ts"
      provides: "Endpoint policy tests for activation lifecycle"
      contains: "activation_limit_reached"
    - path: "worker/src/db/migrations/0002_activation_policy.sql"
      provides: "Any schema/index changes required for one-machine policy"
      contains: "activations"
  key_links:
    - from: "worker/src/routes.ts"
      to: "worker/src/api/activationRoutes.ts"
      via: "versioned route dispatch"
      pattern: "/api/v1/activate"
    - from: "worker/src/api/activationRoutes.ts"
      to: "worker/src/db/repository.ts"
      via: "route handlers delegate policy state to repository"
      pattern: "activateMachine"
---

<objective>
Implement the versioned activation, validation, and deactivation endpoints plus D1 repository operations that enforce exactly one active machine and issue fresh signed entitlement envelopes.

Purpose: Turn the Phase 5 contract into working server behavior while preserving strict one-machine transfer policy and idempotent same-machine retries.
Output: `/api/v1/activate`, `/api/v1/validate`, `/api/v1/deactivate`, repository operations, optional D1 migration, and Worker tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-one-machine-activation-service/05-CONTEXT.md
@.planning/phases/05-one-machine-activation-service/05-RESEARCH.md
@.planning/phases/05-one-machine-activation-service/05-01-SUMMARY.md
@worker/src/api/contracts.ts
@worker/src/api/errors.ts
@worker/src/api/requestValidation.ts
@worker/src/signing/entitlement.ts
@worker/src/db/repository.ts
@worker/src/db/migrations/0001_create_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repository operations for one-machine activation policy</name>
  <files>worker/src/db/repository.ts, worker/src/db/schema.ts, worker/src/db/migrations/0002_activation_policy.sql, tests/worker/repository.test.ts, tests/worker/setup.ts</files>
  <action>Implement repository methods for license lookup by key, active activation lookup, same-machine idempotent activation refresh, different-machine rejection, validate refresh, and matching-machine deactivation. Add a migration only if current schema/indexes are insufficient. Extend MockD1Database to support the new queries and write tests for all policy branches plus simulated concurrent activate attempts.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- repository.test.ts; npm.cmd --prefix worker run typecheck</automated>
  </verify>
  <done>Repository tests prove one active machine is enforced and same-machine retries are idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Implement activate and validate endpoints</name>
  <files>worker/src/routes.ts, worker/src/api/activationRoutes.ts, worker/src/signing/entitlement.ts, tests/worker/activation-routes.test.ts</files>
  <action>Wire `POST /api/v1/activate` and `POST /api/v1/validate`. Both use shared request validation, generic errors, repository policy methods, and entitlement signing. Activation creates or refreshes same-machine activation; validation refreshes existing active activation only. Responses return the full signed entitlement envelope from D-03.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- activation-routes.test.ts</automated>
  </verify>
  <done>Endpoint tests cover valid activation, same-machine retry, different-machine rejection, inactive license, missing license, and validate missing activation.</done>
</task>

<task type="auto">
  <name>Task 3: Implement deactivate endpoint and lifecycle docs</name>
  <files>worker/src/api/activationRoutes.ts, worker/src/routes.ts, tests/worker/activation-routes.test.ts, docs/activation-api.md</files>
  <action>Wire `POST /api/v1/deactivate` requiring licenseKey, matching machineId, requestId, and timestamp. Only matching active activation is deactivated. Update docs with lifecycle examples: activate -> same-machine retry -> validate -> deactivate -> new machine activation.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- activation-routes.test.ts; npm.cmd --prefix worker run typecheck</automated>
  </verify>
  <done>Deactivation tests prove license-key-only cannot deactivate another machine, deactivated machines fail validate, and another machine can activate after deactivation.</done>
</task>

</tasks>

<verification>
- `npm.cmd --prefix worker run typecheck`
- `npm.cmd --prefix worker run test -- repository.test.ts activation-routes.test.ts`
- Confirm route table contains `/api/v1/activate`, `/api/v1/validate`, `/api/v1/deactivate`.
- Confirm tests cover concurrent activation, same-machine idempotency, deactivation, validation refresh, and inactive license failures.
</verification>

<success_criteria>
CLOUD-04, CLOUD-05, CLOUD-06, and CLOUD-07 work through versioned endpoints. CLOUD-10 has automated coverage for activation lifecycle and race/failure policy. No endpoint stores raw secrets or returns policy internals beyond stable error codes.
</success_criteria>

<output>
Create `.planning/phases/05-one-machine-activation-service/05-02-SUMMARY.md` when done.
</output>
