---
phase: 05-one-machine-activation-service
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - worker/src/api/rateLimit.ts
  - worker/src/api/audit.ts
  - worker/src/api/activationRoutes.ts
  - worker/src/env.ts
  - worker/wrangler.toml
  - infra/manifest.yaml
  - infra/deploy.ps1
  - infra/verify.ps1
  - scripts/test-all.ps1
  - scripts/verify-project.ps1
  - tests/worker/abuse.test.ts
  - tests/worker/activation-routes.test.ts
  - docs/activation-api.md
  - docs/cloud-infrastructure.md
autonomous: false
requirements:
  - CLOUD-08
  - CLOUD-09
  - CLOUD-10
requirements_addressed:
  - CLOUD-08
  - CLOUD-09
  - CLOUD-10
user_setup:
  - service: cloudflare
    why: "Rate Limiting bindings and signing secrets may require account/environment configuration for deployed smoke tests"
    env_vars:
      - name: SIGNING_PRIVATE_KEY
        source: "Generated Phase 5 Ed25519 private key fixture or operator-created production key"
        description: "Active private signing key stored as a Worker secret"
must_haves:
  truths:
    - "D-15: Rate limits use layered keys: route + IP, license key hash, and machine ID hash where available."
    - "D-16 and D-17: Audit records are structured and redacted, never raw license keys, machine IDs, secrets, or request bodies."
    - "D-18: Malformed and oversized requests fail before D1 mutation."
    - "D-19: Smoke tests cover happy path and policy failures where locally/deployed feasible."
    - "Worker best practices are preserved: no request-scoped global state, all promises awaited or passed to ctx.waitUntil, Web Crypto for security IDs/signing."
  artifacts:
    - path: "worker/src/api/rateLimit.ts"
      provides: "Layered rate-limit key generation and binding wrapper"
      contains: "licenseKeyHash"
    - path: "worker/src/api/audit.ts"
      provides: "Redacted structured audit event helper"
      contains: "machineIdHash"
    - path: "tests/worker/abuse.test.ts"
      provides: "Malformed, replay, rate-limit, and redaction tests"
      contains: "rate_limited"
    - path: "docs/activation-api.md"
      provides: "Smoke-test and abuse-control documentation"
      contains: "deployed smoke"
  key_links:
    - from: "worker/src/api/activationRoutes.ts"
      to: "worker/src/api/rateLimit.ts"
      via: "route handlers call layered rate limiting before expensive work"
      pattern: "checkActivationRateLimit"
    - from: "worker/src/api/activationRoutes.ts"
      to: "worker/src/api/audit.ts"
      via: "route handlers write redacted audit events for outcomes"
      pattern: "writeActivationAudit"
---

<objective>
Add abuse controls, redacted audit helpers, rate-limit bindings/configuration, failure coverage, deployed/local smoke tests, and final documentation for the Phase 5 activation service.

Purpose: Harden the activation API against probing, replay, malformed requests, and operational blind spots before handing it to native licensing in Phase 6.
Output: Rate-limit and audit modules, Worker config updates, abuse/concurrency tests, smoke-test docs/scripts, and project verification updates.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-one-machine-activation-service/05-CONTEXT.md
@.planning/phases/05-one-machine-activation-service/05-RESEARCH.md
@.planning/phases/05-one-machine-activation-service/05-01-SUMMARY.md
@.planning/phases/05-one-machine-activation-service/05-02-SUMMARY.md
@worker/src/api/activationRoutes.ts
@worker/src/api/contracts.ts
@worker/src/api/errors.ts
@worker/src/db/repository.ts
@worker/wrangler.toml
@infra/manifest.yaml
@docs/cloud-infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layered rate limiting and replay/idempotency coverage</name>
  <files>worker/src/api/rateLimit.ts, worker/src/api/activationRoutes.ts, worker/src/env.ts, worker/wrangler.toml, infra/manifest.yaml, tests/worker/abuse.test.ts</files>
  <action>Add a rate-limit abstraction that can use Cloudflare RateLimit bindings when configured and deterministic no-op/mock behavior in local tests. Build keys from route + IP, license key hash, and machine ID hash where fields are parseable. Ensure request IDs/timestamps are checked for replay before D1 mutation. Add tests for rate_limited and replay_detected outcomes.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- abuse.test.ts; npm.cmd --prefix worker run typecheck</automated>
  </verify>
  <done>Layered rate limiting and replay checks are covered by tests and do not depend on raw license or machine identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement redacted structured activation audit helper</name>
  <files>worker/src/api/audit.ts, worker/src/api/activationRoutes.ts, worker/src/db/repository.ts, tests/worker/abuse.test.ts</files>
  <action>Create audit helpers that hash license keys and machine IDs before storing details. Record action, outcome code, activation ID when known, request ID, route, coarse client info, and timestamp. Ensure no raw request body, raw license key, raw machine ID, customer PII, or secret can be written through the helper. Prefer `ctx.waitUntil` only for non-critical audit writes and never float promises.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- abuse.test.ts repository.test.ts</automated>
  </verify>
  <done>Audit tests prove redaction and structured fields for activate, validate, deactivate, malformed, rejected, and rate-limited outcomes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify smoke tests, docs, and Phase 5 handoff</name>
  <files>scripts/test-all.ps1, scripts/verify-project.ps1, docs/activation-api.md, docs/cloud-infrastructure.md, infra/deploy.ps1, infra/verify.ps1</files>
  <action>Update verification scripts and docs for Phase 5. Add local/deployed smoke instructions covering activate, same-machine retry, validate, deactivate, second-machine rejection, inactive license, malformed request, and rate-limit behavior where feasible. Ensure test-all runs Worker typecheck/tests. Run project verifier and Worker suite. If deployed smoke cannot run without real Cloudflare/Lemon configuration, document the exact limitation and local substitute; do not claim deployed pass without evidence.</action>
  <verify>
    <automated>npm.cmd --prefix worker run typecheck; npm.cmd --prefix worker run test; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify-project.ps1 -SelfTest</automated>
    <human-check>Review docs/activation-api.md and docs/cloud-infrastructure.md for Phase 5 setup/smoke clarity. Type "approved" when acceptable, or describe required corrections.</human-check>
  </verify>
  <done>Worker tests and project verifier pass, smoke docs are honest about local vs deployed coverage, and human approval is recorded.</done>
</task>

</tasks>

<verification>
- `npm.cmd --prefix worker run typecheck`
- `npm.cmd --prefix worker run test`
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify-project.ps1 -SelfTest`
- Manual review of Phase 5 local/deployed smoke docs.
</verification>

<success_criteria>
CLOUD-08, CLOUD-09, and CLOUD-10 are covered by code, tests, docs, and smoke instructions. Rate limits, audit redaction, request validation, signing rotation metadata, and failure tests are complete enough for Phase 6 native offline licensing to consume the service contract.
</success_criteria>

<output>
Create `.planning/phases/05-one-machine-activation-service/05-03-SUMMARY.md` when done.
</output>
