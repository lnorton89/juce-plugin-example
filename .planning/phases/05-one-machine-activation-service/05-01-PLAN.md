---
phase: 05-one-machine-activation-service
plan: 01
type: execute
wave: 1
depends_on:
  - "04-04"
files_modified:
  - worker/src/api/contracts.ts
  - worker/src/api/errors.ts
  - worker/src/api/requestValidation.ts
  - worker/src/signing/canonicalJson.ts
  - worker/src/signing/entitlement.ts
  - worker/src/env.ts
  - tests/worker/contracts.test.ts
  - tests/worker/signing.test.ts
  - tests/worker/fixtures/entitlement-v1.json
  - docs/activation-api.md
  - infra/manifest.yaml
autonomous: true
requirements:
  - CLOUD-04
  - CLOUD-08
  - CLOUD-09
requirements_addressed:
  - CLOUD-04
  - CLOUD-08
  - CLOUD-09
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 5 API contract defines POST /api/v1/activate, /api/v1/validate, and /api/v1/deactivate."
    - "D-02: No embedded app secret is required or documented; request protection uses license/machine proof plus replay/rate/audit controls."
    - "D-03: Successful activation contract returns activationId, entitlementToken, tokenType, expiresAt, serverTime, and refreshAfter."
    - "D-04: Error model exposes stable machine-readable codes and generic messages only."
    - "D-05: Request schemas use licenseKey, machineId, requestId, timestamp, and optional appVersion."
    - "D-10 through D-14: Signing uses canonical JSON plus detached Ed25519 signature, minimal claims, kid, and public key ring metadata."
  artifacts:
    - path: "worker/src/api/contracts.ts"
      provides: "Versioned activation API request and response TypeScript contracts"
      contains: "ActivateRequest"
    - path: "worker/src/api/errors.ts"
      provides: "Stable activation error code model and generic error envelope builder"
      contains: "activation_limit_reached"
    - path: "worker/src/signing/entitlement.ts"
      provides: "Canonical entitlement payload builder and Ed25519 signing surface"
      contains: "kid"
    - path: "tests/worker/fixtures/entitlement-v1.json"
      provides: "Cross-language entitlement fixture for Phase 6 native verification"
      contains: "schemaVersion"
    - path: "docs/activation-api.md"
      provides: "Human-readable API and token contract"
      contains: "/api/v1/activate"
  key_links:
    - from: "worker/src/api/contracts.ts"
      to: "docs/activation-api.md"
      via: "documented request/response schemas mirror TypeScript types"
      pattern: "ActivateResponse"
    - from: "worker/src/signing/canonicalJson.ts"
      to: "tests/worker/fixtures/entitlement-v1.json"
      via: "fixture locks canonical bytes and signature expectations"
      pattern: "canonical"
---

<objective>
Define the Phase 5 activation API contracts, generic error model, request validation primitives, canonical entitlement token shape, Ed25519 signing surface, public-key-ring metadata, and cross-language fixtures.

Purpose: Lock the API/token contract before endpoint policy is implemented so Phase 5 and Phase 6 share one verifiable schema.
Output: TypeScript contract modules, request validation helpers, entitlement signing helpers, signing fixtures, key-ring config documentation, and `docs/activation-api.md`.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-one-machine-activation-service/05-CONTEXT.md
@.planning/phases/05-one-machine-activation-service/05-RESEARCH.md
@worker/src/routes.ts
@worker/src/env.ts
@worker/src/db/schema.ts
@worker/src/db/repository.ts
@infra/manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define versioned API contracts and generic errors</name>
  <files>worker/src/api/contracts.ts, worker/src/api/errors.ts, docs/activation-api.md, tests/worker/contracts.test.ts</files>
  <action>Create TypeScript request/response contracts for activate, validate, deactivate, common request metadata, entitlement envelope, and generic error envelopes. Define error codes: invalid_request, license_not_found, license_inactive, activation_limit_reached, activation_not_found, machine_mismatch, rate_limited, replay_detected, server_error. Add tests that ensure all documented codes are stable and that response builders never include internal details.</action>
  <verify>
    <automated>npm.cmd --prefix worker run typecheck; npm.cmd --prefix worker run test -- contracts.test.ts</automated>
  </verify>
  <done>Contracts and docs define the stable v1 API without any embedded app-secret field.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded request validation primitives</name>
  <files>worker/src/api/requestValidation.ts, worker/src/api/contracts.ts, tests/worker/contracts.test.ts</files>
  <action>Implement JSON request parsing and validation for method/content-type/body-size/schema/string-length/requestId/timestamp skew. Support minimal fields: licenseKey, machineId, requestId, timestamp, optional appVersion. Validation must return generic error envelopes and must be reusable by all three endpoints before D1 mutation.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- contracts.test.ts</automated>
  </verify>
  <done>Malformed, oversized, stale, and overlong requests fail before repository calls.</done>
</task>

<task type="auto">
  <name>Task 3: Implement canonical entitlement signing contract and fixtures</name>
  <files>worker/src/signing/canonicalJson.ts, worker/src/signing/entitlement.ts, worker/src/env.ts, infra/manifest.yaml, tests/worker/signing.test.ts, tests/worker/fixtures/entitlement-v1.json, docs/activation-api.md</files>
  <action>Implement deterministic canonical JSON for the allowed entitlement payload shape, Ed25519 detached signing using Web Crypto, base64url encoding helpers, minimal claims, and kid support. Extend environment/config docs for active private key and non-secret public key ring. Generate a deterministic fixture with payload, canonical string/bytes representation, signature, kid, and public key for future native tests.</action>
  <verify>
    <automated>npm.cmd --prefix worker run test -- signing.test.ts; npm.cmd --prefix worker run typecheck</automated>
  </verify>
  <done>Signing tests prove canonical output is deterministic and fixture verification passes without customer PII or raw license keys.</done>
</task>

</tasks>

<verification>
- `npm.cmd --prefix worker run typecheck`
- `npm.cmd --prefix worker run test -- contracts.test.ts signing.test.ts`
- Inspect `docs/activation-api.md` for versioned endpoints and stable error code table.
- Inspect `tests/worker/fixtures/entitlement-v1.json` for no raw license key, no customer email/name, and minimal claims only.
</verification>

<success_criteria>
All activation API and entitlement-token decisions D-01 through D-05 and D-10 through D-14 are represented in code, docs, and tests. No app secret is introduced. The signing fixture is ready for Phase 6 native verification.
</success_criteria>

<output>
Create `.planning/phases/05-one-machine-activation-service/05-01-SUMMARY.md` when done.
</output>
