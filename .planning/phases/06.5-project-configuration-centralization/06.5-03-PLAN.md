---
phase: 06.5-project-configuration-centralization
plan: 03
type: execute
wave: 2
depends_on: ["06.5-01"]
files_modified:
  - scripts/generate-config-ts.mjs
  - ui/src/config/projectConfig.ts
  - ui/src/bridge/protocol.ts
  - ui/src/components/BrandHeader.tsx
  - ui/src/components/StatusFooter.tsx
  - ui/src/components/BridgeStatus.tsx
  - CMakePresets.json
  - tests/fixtures/bridge/host-info-v1.json
  - docs/development.md
  - docs/bridge-protocol.md
  - docs/vst3-smoke-test.md
  - docs/troubleshooting.md
  - scripts/bootstrap.ps1
  - README.md
autonomous: true
requirements:
  - CFG-04
  - CFG-06
  - CFG-09
user_setup: []
must_haves:
  truths:
    - "Changing product name in project-config.json updates the React/MUI UI header, footer, and error messages"
    - "Changing company name in project-config.json updates the footer and bridge protocol validation"
    - "A single documented command regenerates all config-derived constant files"
    - "README.md and docs reference product name and version from config as template variables"
  artifacts:
    - path: "ui/src/config/projectConfig.ts"
      provides: "TypeScript config module with tree-shakeable exports"
      exports: ["productName", "companyName", "version", "urls", "errorTemplates"]
    - path: "scripts/generate-config-ts.mjs"
      provides: "Visual Studio Code/Node script that generates TypeScript config from project-config.json"
      min_lines: 40
    - path: "ui/src/bridge/protocol.ts"
      provides: "Bridge protocol type definitions using config-derived values for product name and company name"
      contains: "import { productName, companyName } from '../config/projectConfig'"
  key_links:
    - from: "ui/src/config/projectConfig.ts" to "project-config.json" via: "import assertion or generated from scripts/generate-config-ts.mjs"
    - from: "ui/src/bridge/protocol.ts" to "ui/src/config/projectConfig.ts" via: "import { productName, companyName }"
    - from: "scripts/generate-config-ts.mjs" to "project-config.json" via: "readFileSync"
---

<objective>
**Fix:** React/MUI UI components, bridge protocol type definitions, CMake presets, and documentation files contain hardcoded product identity strings that should come from the centralized config.

**What this plan delivers:** TypeScript/React components consume config-derived values through a generated `.ts` module. Bridge protocol types use config values instead of string literals. A regeneration script ensures all derived files stay in sync. Documentation uses template variable conventions. After this plan, changing any value in `project-config.json` propagates to ALL source files across C++, TypeScript, Worker, CMake, and scripts.

**MVP vertical slice:** After this plan, a developer can edit the product name in `project-config.json`, run one regeneration command, and see the new name everywhere — the UI header, status footer, error messages, bridge protocol validation, documentation, and all build artifacts.
</objective>

<execution_context>
@C:/Users/Lawrence/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-project-configuration-centralization/06.5-CONTEXT.md
@.planning/phases/06.5-project-configuration-centralization/06.5-RESEARCH.md
@.planning/phases/06.5-project-configuration-centralization/06.5-01-PLAN.md
@.planning/phases/06.5-project-configuration-centralization/06.5-02-PLAN.md

# Current UI components with hardcoded values
@C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/bridge/protocol.ts
@C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/BrandHeader.tsx
@C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/StatusFooter.tsx
@C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/BridgeStatus.tsx
@C:/Users/Lawrence/Documents/Dev/juce-plugin-example/CMakePresets.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript config generation script, generate projectConfig.ts module, and update UI components</name>
  <files>
    scripts/generate-config-ts.mjs
    ui/src/config/projectConfig.ts
    ui/src/bridge/protocol.ts
    ui/src/components/BrandHeader.tsx
    ui/src/components/StatusFooter.tsx
    ui/src/components/BridgeStatus.tsx
    tests/fixtures/bridge/host-info-v1.json
  </files>
  <read_first>
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/.planning/phases/06.5-project-configuration-centralization/06.5-RESEARCH.md (Pattern 4 lines 306-347 — TypeScript generation)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/project-config.json (created in Plan 06.5-01 — config source of truth)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/bridge/protocol.ts (lines 15-16 — productName, companyName literal types)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/BrandHeader.tsx (line 24 — "LumaScope" header text)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/StatusFooter.tsx (line 37 — "Signal Foundry Audio" footer text)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/src/components/BridgeStatus.tsx (line 27 — "reopen LumaScope" in error text const)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/ui/tsconfig.json (check for resolveJsonModule setting)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/tests/fixtures/bridge/host-info-v1.json (lines 1-9 — fixture data)
  </read_first>
  <action>
    Per D-23 (TypeScript config generation), D-24 (UI component updates), D-25 (bridge protocol updates):

    **1. Read `ui/tsconfig.json`** — if `resolveJsonModule` is not `true` in `compilerOptions`, add it.

    **2. Create `scripts/generate-config-ts.mjs`**
    A Node.js script that reads `project-config.json` and generates `ui/src/config/projectConfig.ts`. Following RESEARCH.md Pattern 4 (lines 311-337), the script must:
    - Import `readFileSync` and `writeFileSync` from `'fs'`, `resolve` and `dirname` from `'path'`, `fileURLToPath` from `'url'`
    - Resolve the repo root using `import.meta.url` and `fileURLToPath`
    - Read `project-config.json` via `readFileSync` and `JSON.parse`
    - Build a string with `export const` for: `productName`, `companyName`, `version`, `brandHeaderText`, `tagline`, `windowTitle`, `appdataDirName` — each using `JSON.stringify(config.X.Y)` with `as const` suffix
    - Build `export const urls` object with `devServer` and `api` fields
    - Build `export const errorTemplates` object with fields from `config.ui.errorMessageTemplates`
    - Write the output to `ui/src/config/projectConfig.ts` via `writeFileSync`
    - Print a success message to console
    The script must prefix the output with a comment: "Generated from project-config.json — do not edit manually. Run: node scripts/generate-config-ts.mjs"

    **3. Run the generation script to create `ui/src/config/projectConfig.ts`**
    Execute: `node scripts/generate-config-ts.mjs`

    **4. Modify `ui/src/bridge/protocol.ts`**
    Replace the hardcoded literal types with config-driven values:
    - Add import at the top: `import { productName, companyName } from '../config/projectConfig';`
    - Line 15: Change `productName: 'LumaScope'` to `productName: typeof productName`
    - Line 16: Change `companyName: 'Signal Foundry Audio'` to `companyName: typeof companyName`
    - Line 82: Change `item.productName === 'LumaScope'` to `item.productName === productName`
    - Line 83: Change `item.companyName === 'Signal Foundry Audio'` to `item.companyName === companyName`
    Keep the `as const` exports for protocol event strings (`uiReadyEvent`, `hostInfoEvent`, etc.) — those are protocol identifiers, not product config.

    **5. Modify `ui/src/components/BrandHeader.tsx`**
    - Add import: `import { brandHeaderText, tagline } from '../config/projectConfig';`
    - Line 24: Replace `>LumaScope<` with `>{brandHeaderText}<`
    - Line 26: Replace `>Spectrum analyzer<` with `>{tagline}<`

    **6. Modify `ui/src/components/StatusFooter.tsx`**
    - Add import: `import { companyName } from '../config/projectConfig';`
    - Line 37: Replace `>Signal Foundry Audio<` with `>{companyName}<`

    **7. Modify `ui/src/components/BridgeStatus.tsx`**
    - Add import: `import { productName } from '../config/projectConfig';`
    - Line 27: The `copyByKind` object has a `'runtime-error'` field containing `'reopen LumaScope.'`. Replace the entire field value with a template literal: `` `The interface could not start. Check the WebView2 runtime and reopen ${productName}.` ``
    Since `copyByKind` is `as const`, the template literal must be evaluated at module scope. Ensure the `productName` value is available at module initialization time (it's imported as a constant, so it will be).

    **8. Create `tests/fixtures/bridge/FIXTURES.md`**
    Create a short document explaining that fixture values (`productName`, `companyName`, `productVersion`) should match `project-config.json`. Note that when config values change, fixtures must be regenerated manually. List the specific fixtures affected: `host-info-v1.json`, and any other files in `tests/fixtures/bridge/` that contain hardcoded product identity values.
  </action>
  <verify>
    <automated>node scripts/generate-config-ts.mjs 2>&1; if ($?) { Write-Host "TS gen OK" } else { Write-Host "FAIL" }; npm --prefix ui run build 2>&1; if ($?) { Write-Host "UI build OK" } else { Write-Host "UI build FAIL" }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-config-ts.mjs` runs successfully: `node scripts/generate-config-ts.mjs` exits 0
    - `ui/src/config/projectConfig.ts` is generated with `productName`, `companyName`, `version`, `brandHeaderText`, `tagline`, `urls`, `errorTemplates` exports
    - `ui/src/bridge/protocol.ts` imports `productName` and `companyName` from config module and uses `typeof productName`, `typeof companyName` in HostInfo interface
    - `protocol.ts` uses `productName` and `companyName` variables in `parseHostInfo()` comparisons (not string literals)
    - `ui/src/components/BrandHeader.tsx` displays `{brandHeaderText}` instead of hardcoded "LumaScope"
    - `ui/src/components/StatusFooter.tsx` displays `{companyName}` instead of hardcoded "Signal Foundry Audio"
    - `ui/src/components/BridgeStatus.tsx` uses template literal `${productName}` in the runtime-error message
    - Full UI build succeeds: `npm --prefix ui run build` exits 0
    - TypeScript compilation passes (no type errors from changed literal types)
  </acceptance_criteria>
  <done>TypeScript config generation script and module exist; all UI components and bridge protocol use config-driven values; UI build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Update CMakePresets.json and documentation with config-driven values</name>
  <files>
    CMakePresets.json
    README.md
    docs/development.md
    docs/bridge-protocol.md
    docs/vst3-smoke-test.md
    docs/troubleshooting.md
  </files>
  <read_first>
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/CMakePresets.json (lines 9-14 — cache variables with LUMASCOPE_* names)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/README.md (lines 1-175 — product name, target names, build commands)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/docs/development.md (lines 1-53 — build commands and target names)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/docs/bridge-protocol.md (lines 1-27 — bridge protocol values)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/docs/vst3-smoke-test.md (lines 1-32 — VST3 test commands)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/docs/troubleshooting.md (lines 1-65 — troubleshooting commands)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/project-config.json (source of truth)
  </read_first>
  <action>
    Per D-26 (CMake preset updates), D-27 (documentation template conventions):

    **1. Modify `CMakePresets.json`**
    The `LUMASCOPE_*` cache variable names are CMake internal variable PREFIXES (defined by `env.envVarPrefix` in config). These are not user-facing config values — they are the mechanism for passing values, not the values themselves. However, the default values should reference config:
    - Keep the cache variable entries (`LUMASCOPE_DEPENDENCY_DIR`, `LUMASCOPE_WEBVIEW_DEV_SERVER`, `LUMASCOPE_RELEASE_BUILD`) as-is — they are environment/override points for developer workflows.
    - Add a comment (JSON doesn't support comments, so add a `description` or use the existing structure): The cache variable names use `${PROJECT_CONFIG_ENV_PREFIX}` format. Update preset descriptions to say "For product identity values, see project-config.json."
    - Since JSON doesn't support comments, add descriptive `description` fields to each preset explaining what values are config-driven.

    **2. Create `docs/CONFIG-REFERENCE.md`**
    A new document explaining the configuration system. The document must contain these sections:
    - **# Project Configuration Reference** (H1 title)
    - **## Centralized Configuration** describing that all product identity values are defined in `project-config.json` at the project root, with schema in `project-config.schema.json` and every value's `filesAffected` array listing every source file it affects
    - **## Quick Start** with numbered steps: (1) Edit `project-config.json` to change product name, company name, version, URLs, etc. (2) Run `node scripts/generate-config-ts.mjs` to regenerate TypeScript constants (3) Reconfigure with `cmake --preset vs2019-debug` to generate C++ header and PowerShell module (4) Rebuild with `cmake --build --preset vs2019-debug --target {standalone_target} --parallel 4`
    - **## Regeneration (CFG-09)** showing the command: `node scripts/generate-config-ts.mjs && cmake --preset vs2019-debug`
    - **## Config Value Categories** as a table with columns Category and Example Values, with rows for: product (name, companyName, copyrightYear), version (string 0.1.0, major, minor, patch), identifiers (bundleId, namespace, plugin codes), paths (appdataDirName, sourcePreferenceFilename), and a note "see project-config.json for full list"
    - **## Template Variables in Documentation** explaining that documentation uses `{{variable_name}}` syntax, with examples `{{product_name}}` → LumaScope, `{{company_name}}` → Signal Foundry Audio, noting these are documented conventions for manual replacement, not automated substitution

    **3. Update documentation files**

    For ALL doc files, replace hardcoded product references with the `{{variable_name}}` convention AND show the resolved value in parentheses for clarity. Per D-29 (deferred automation), manual convention is the approach:

    - `README.md`: Replace "LumaScope" (as the product name in prose) with "{{product_name}} (LumaScope)" in first reference, then keep as "{{product_name}}" for subsequent. Replace version references with "{{version}} (0.1.0)". Replace target names with "{{vst3_target}} (LumaScope_VST3)", "{{standalone_target}} (LumaScope_Standalone)". Replace build directory paths that contain version strings with template versions.
    - `docs/development.md`: Same pattern — replace hardcoded target names, version strings, and product names with template variables + resolved values.
    - `docs/bridge-protocol.md`: Replace `"LumaScope"` in JSON examples with `"{{product_name}}"` and note: "Values shown are from project-config.json."
    - `docs/vst3-smoke-test.md`: Replace target names, artifact paths with template variables.
    - `docs/troubleshooting.md`: Replace product name in error descriptions with template variables.

    **4. Specific replacements for `docs/bridge-protocol.md`**
    - Line 22: `"productName": "LumaScope"` → `"productName": "{{product_name}}"` (with a note: "Value from project-config.json")
    - Line 23: `"companyName": "Signal Foundry Audio"` → `"companyName": "{{company_name}}"`
    - Line 24: `"productVersion": "0.1.0"` → `"productVersion": "{{version}}"`
    - Line 27: `"buildMarker": "0.1.0-Debug"` → `"buildMarker": "{{version}}-<config>"`

    Do NOT change any documentation structure or formatting — only the specific values that should come from config.
  </action>
  <verify>
    <automated># Verify template variable conventions are established
grep -q "{{product_name}}" README.md; if ($?) { Write-Host "README has template vars OK" } else { Write-Host "README missing templates" }
grep -q "{{version}}" docs/development.md; if ($?) { Write-Host "Docs have template vars OK" } else { Write-Host "Docs missing templates" }
# Verify CONFIG-REFERENCE.md exists
Test-Path docs/CONFIG-REFERENCE.md; if ($?) { Write-Host "Config ref doc exists" } else { Write-Host "Missing CONFIG-REFERENCE.md" }</automated>
  </verify>
  <acceptance_criteria>
    - `docs/CONFIG-REFERENCE.md` exists explaining the config system, regeneration command, and template variable convention
    - `README.md` references `{{product_name}}`, `{{version}}`, `{{vst3_target}}`, `{{standalone_target}}` template variables (with resolved values in parentheses)
    - `docs/development.md` uses template variable conventions for target names and build commands
    - `docs/bridge-protocol.md` uses `{{product_name}}`, `{{company_name}}`, `{{version}}` in JSON examples
    - `docs/vst3-smoke-test.md` and `docs/troubleshooting.md` use template variables for hardcoded product names and target names
    - `CMakePresets.json` has description fields noting that config values come from `project-config.json`
  </acceptance_criteria>
  <done>Documentation uses {{variable}} template conventions referencing project-config.json; CONFIG-REFERENCE.md documents the config system and regeneration commands</done>
</task>

<task type="auto">
  <name>Task 3: Create regeneration command and verify end-to-end config propagation (CFG-09)</name>
  <files>
    scripts/generate-all-config.mjs
    package.json
  </files>
  <read_first>
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/.planning/phases/06.5-project-configuration-centralization/06.5-RESEARCH.md (Validation Architecture lines 908-941)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/package.json (check for existing scripts section)
    C:/Users/Lawrence/Documents/Dev/juce-plugin-example/project-config.json (config source of truth)
  </read_first>
  <action>
    Per D-28 (CFG-09: regeneration command):

    **1. Create `scripts/generate-all-config.mjs`**
    A single command that regenerates ALL config-derived files. This is the canonical regeneration script required by CFG-09. The script must:
    - Start with `#!/usr/bin/env node` shebang
    - Import `execSync` from `'child_process'`, `resolve` from `'path'`, `fileURLToPath` from `'url'`, `readFileSync` from `'fs'`
    - Resolve the repo root using `import.meta.url` and `fileURLToPath`
    - Read `project-config.json` via `readFileSync` and `JSON.parse` to display a banner like `Regenerating config-driven files for {product.name} v{version.string}`
    - Accept optional `--cmake-preset=` argument (default: `'vs2019-debug'`)
    - Step 1: Run `node scripts/generate-config-ts.mjs` via `execSync` with `{ cwd: root, stdio: 'inherit' }`. On failure: log error and `process.exit(1)`
    - Step 2: Run `cmake --preset {presetName}` via `execSync` with `{ cwd: root, stdio: 'inherit' }`. On failure: log error and `process.exit(1)`
    - On success: print completion message with build command hint

    **2. Update `package.json`**
    Create or update a root-level `package.json` with the following properties: `name: "juce-plugin-example"`, `private: true`. In the `scripts` object, add three entries: `"generate-config": "node scripts/generate-config-ts.mjs"`, `"generate-all-config": "node scripts/generate-all-config.mjs"`, `"validate-config": "node scripts/validate-config.mjs"`.
    If `package.json` already exists, merge these scripts into the existing `scripts` section without removing any existing entries.

    **3. Verify end-to-end config propagation**
    Perform a manual verification (documented in acceptance_criteria):
    - Run `node scripts/generate-all-config.mjs --cmake-preset=vs2019-debug`
    - Verify it completes without errors
    - Check that `ui/src/config/projectConfig.ts` exists and contains the current product name
    - Check that CMake configure succeeded (generated build tree exists)

    **4. (Optional) Add a comment block to `project-config.json`**
    Add a `_comment` field at the top level of `project-config.json` with the value: `"Project configuration — edit values here to rebrand or reconfigure. After editing, run: node scripts/generate-all-config.mjs"`
    This makes the developer workflow discoverable directly in the config file (JSON schema tools should tolerate the `_comment` key).
  </action>
  <verify>
    <automated>node scripts/generate-all-config.mjs --cmake-preset=vs2019-debug 2>&1; if ($?) { Write-Host "Regeneration OK" } else { Write-Host "Regeneration FAIL" }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-all-config.mjs` exists and is executable
    - Running `node scripts/generate-all-config.mjs --cmake-preset=vs2019-debug` exits 0
    - The script regenerates `ui/src/config/projectConfig.ts` via `generate-config-ts.mjs`
    - The script reconfigures CMake (generating C++ header and PowerShell module)
    - `package.json` has `generate-config`, `generate-all-config`, and `validate-config` scripts in the `scripts` section
    - `project-config.json` has a `_comment` field explaining the regeneration command
    - Test: Change a value in `project-config.json` (e.g., `brandHeaderText`), run `generate-all-config`, verify the UI TypeScript module has the new value — document this test in acceptance criteria
  </acceptance_criteria>
  <done>generate-all-config.mjs script exists and regenerates all config-driven files in one command; package.json has config-related scripts; end-to-end config propagation is verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-config.json → TypeScript config module | Config values become compile-time constants in UI bundle; values are identical to previously hardcoded strings |
| project-config.json → CMakePresets.json | Cache variable defaults reference config values; no new risk over previous hardcoded presets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-07 | Tampering | ui/src/config/projectConfig.ts | mitigate | Generated file is gitignored (or committed but regenerated on configure); tampering detected by git diff or build mismatch |
| T-06.5-08 | Information Disclosure | docs/CONFIG-REFERENCE.md | accept | Configuration documentation is intentionally informative; contains no secrets |
| T-06.5-09 | Tampering | scripts/generate-all-config.mjs | mitigate | Script is versioned in git; tampering detected by git diff |
| T-06.5-SC | Tampering | project-config.json (when documentation references it) | mitigate | Config file is versioned in git; schema validation runs at build time |
</threat_model>

<verification>
Run sequentially (each on separate line or with `; if ($?) { ... }`):
1. `node scripts/generate-config-ts.mjs` — must exit 0
2. `npm --prefix ui run build` — must exit 0 (TypeScript compilation and bundling)
3. `node scripts/generate-all-config.mjs --cmake-preset=vs2019-debug` — must exit 0
4. Verify TypeScript config module exports: `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));console.log(Object.keys(p.scripts||{}).join(', '))"` and check output contains "generate-config", "generate-all-config", "validate-config"
5. Verify documentation template variables: `grep -q "{{product_name}}" README.md` (exit 0) and `grep -q "{{version}}" README.md` (exit 0)
</verification>

<success_criteria>
1. `scripts/generate-config-ts.mjs` generates a valid TypeScript module in `ui/src/config/projectConfig.ts`
2. All UI components (BrandHeader, StatusFooter, BridgeStatus, protocol.ts) import and display config-driven values
3. Full UI build succeeds (`npm --prefix ui run build`)
4. `docs/CONFIG-REFERENCE.md` documents the config system, regeneration command, and template variable conventions
5. All documentation files use `{{variable_name}}` template conventions with resolved values where applicable
6. `scripts/generate-all-config.mjs` successfully regenerates both TypeScript and CMake config in one command
7. `package.json` has all config-related scripts
8. `project-config.json` has a `_comment` field pointing to the regeneration command
9. End-to-end: change `brandHeaderText` in `project-config.json` → run `node scripts/generate-all-config.mjs` → UI build shows new header text
</success_criteria>

<output>
Create `.planning/phases/06.5-project-configuration-centralization/06.5-03-SUMMARY.md` when done.
Document the TypeScript generation approach, the bridge protocol type preservation strategy (typeof productName maintains literal narrowing), the documentation template convention, and the single regeneration command that fulfills CFG-09.
</output>
