---
phase: 02-end-to-end-vst3-analyzer
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - plugin/include/LumaScope/HostBridge.h
  - plugin/source/HostBridge.cpp
  - plugin/include/LumaScope/PluginEditor.h
  - plugin/source/PluginEditor.cpp
  - ui/src/bridge/protocol.ts
  - ui/src/bridge/BridgeProvider.tsx
  - ui/src/components/AnalyzerStage.tsx
  - ui/src/components/SpectrumCanvas.tsx
  - ui/src/components/SpectrumCanvas.test.tsx
  - ui/src/bridge/SpectrumSnapshot.test.ts
  - tests/fixtures/bridge/spectrum-snapshot-v1.json
  - docs/bridge-protocol.md
autonomous: true
requirements:
  - DSP-03
  - DSP-05
  - VST3-01
  - VST3-03
  - UI-02
requirements_addressed:
  - DSP-03
  - DSP-05
  - VST3-01
  - VST3-03
  - UI-02
user_setup: []
must_haves:
  truths:
    - "D-05 and D-07: Editor consumes only the newest complete snapshot at a bounded display rate, not every analyzer frame."
    - "D-08: Reopened editor reads the latest processor-owned snapshot quickly without restarting audio processing."
    - "D-09: The first real spectrum renders as a smooth filled spectral curve with subtle glow/fill in the existing dark LumaScope instrument-panel look."
    - "D-10: Renderer uses canvas or another bounded-node strategy; it does not create one DOM/MUI element per FFT bin."
    - "D-11: Existing central analyzer stage remains the visual home and preserves room for future labels/controls without implementing v2 controls."
    - "UI-02: Spectrum renders smoothly at a bounded display frame rate."
  artifacts:
    - path: "plugin/source/HostBridge.cpp"
      provides: "Native protocol-v1 `spectrum.snapshot` payload builder and validation"
      contains: "spectrum.snapshot"
    - path: "plugin/source/PluginEditor.cpp"
      provides: "Timer-side snapshot polling and WebView event emission"
      contains: "emitEventIfBrowserIsVisible"
    - path: "ui/src/bridge/protocol.ts"
      provides: "TypeScript SpectrumSnapshot schema/parser"
      exports: ["SpectrumSnapshot", "parseSpectrumSnapshot"]
    - path: "ui/src/bridge/BridgeProvider.tsx"
      provides: "Bridge state subscription for latest spectrum snapshot"
    - path: "ui/src/components/SpectrumCanvas.tsx"
      provides: "Bounded canvas filled-curve renderer"
    - path: "tests/fixtures/bridge/spectrum-snapshot-v1.json"
      provides: "Shared snapshot fixture"
  key_links:
    - from: "plugin/source/PluginEditor.cpp"
      to: "plugin/include/LumaScope/PluginProcessor.h"
      via: "message-thread latest snapshot polling"
      pattern: "readLatest"
    - from: "plugin/source/HostBridge.cpp"
      to: "ui/src/bridge/protocol.ts"
      via: "closed schema `spectrum.snapshot` event"
      pattern: "spectrum.snapshot"
    - from: "ui/src/components/AnalyzerStage.tsx"
      to: "ui/src/components/SpectrumCanvas.tsx"
      via: "#spectrum-renderer-mount visual home"
      pattern: "SpectrumCanvas"
---

<objective>
Extend the typed native/web bridge and existing central stage to display live analyzer snapshots as an efficient filled spectrum curve for DSP-03, DSP-05, VST3-01, VST3-03, and UI-02, covering D-05 and D-07 through D-11.

Purpose: Make the VST3 visibly useful in the WebView without violating the audio-thread boundary.
Output: `spectrum.snapshot` protocol extension, bounded editor polling, React snapshot state, canvas renderer, fixtures/tests, and bridge documentation.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-end-to-end-vst3-analyzer/02-CONTEXT.md
@.planning/phases/02-end-to-end-vst3-analyzer/02-RESEARCH.md
@.planning/phases/02-end-to-end-vst3-analyzer/02-01-SUMMARY.md
@.planning/phases/02-end-to-end-vst3-analyzer/02-02-SUMMARY.md
@docs/bridge-protocol.md
@plugin/include/LumaScope/HostBridge.h
@plugin/source/HostBridge.cpp
@plugin/include/LumaScope/PluginEditor.h
@plugin/source/PluginEditor.cpp
@ui/src/bridge/protocol.ts
@ui/src/bridge/BridgeProvider.tsx
@ui/src/components/AnalyzerStage.tsx
</context>

<phase_goal>
A user can insert the VST3 on audio, hear unchanged output, and see a correct, smooth logarithmic spectrum.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed spectrum snapshot bridge event</name>
  <files>plugin/include/LumaScope/HostBridge.h, plugin/source/HostBridge.cpp, tests/fixtures/bridge/spectrum-snapshot-v1.json, ui/src/bridge/protocol.ts, ui/src/bridge/SpectrumSnapshot.test.ts, docs/bridge-protocol.md</files>
  <behavior>
    - Test 1: Valid `spectrum.snapshot` fixture parses in TypeScript and contains protocol version, sequence, profile, sample rate, FFT size, Hz/dB bounds, and bounded bins.
    - Test 2: Invalid profile names, wrong protocol version, non-finite numbers, oversized bin arrays, and malformed bins are rejected.
    - Test 3: Native payload builder emits the same closed field names and bounded values documented in `docs/bridge-protocol.md`.
  </behavior>
  <action>Extend protocol v1 with a closed `spectrum.snapshot` event. Add a native event identifier/payload builder that converts `SpectrumSnapshot` to bounded `juce::var` only on the message/editor side, never in `processBlock` per D-06. Add TypeScript `SpectrumSnapshot` types and parser tests using the shared fixture. Document the new event and versioning rules in `docs/bridge-protocol.md`.</action>
  <verify>
    <automated>npm.cmd --prefix ui run test:run; cmake --build --preset vs2019-debug --target LumaScopeNativeTests --parallel 4; ctest --preset vs2019-debug --output-on-failure</automated>
  </verify>
  <done>Native and TypeScript protocol layers agree on the snapshot schema, reject malformed data, and preserve Phase 1 closed-schema bridge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poll latest snapshots from editor at bounded cadence</name>
  <files>plugin/include/LumaScope/PluginEditor.h, plugin/source/PluginEditor.cpp, plugin/source/HostBridge.cpp</files>
  <behavior>
    - Test 1: Editor emits at most the active profile display cadence, around 30 FPS for Musical per D-03 and D-07.
    - Test 2: If multiple snapshots arrive between timer ticks, only the newest sequence is emitted per D-05.
    - Test 3: Closing and recreating the editor does not reset processor analyzer state, and the new editor can emit the latest snapshot per D-08.
  </behavior>
  <action>Reuse the existing editor timer boundary for bridge readiness and add a separate bounded snapshot polling path after handshake readiness. Read from the processor's latest-snapshot API on the message thread, emit `spectrum.snapshot` only when a newer sequence exists, and never emit from `processBlock`. Keep Debug-only smoke hooks Debug-only. Avoid changing Vite/embedded URL behavior.</action>
  <verify>
    <automated>cmake --preset vs2019-debug; cmake --build --preset vs2019-debug --target LumaScope_Standalone LumaScope_VST3 LumaScopeNativeTests --parallel 4; ctest --preset vs2019-debug --output-on-failure</automated>
  </verify>
  <done>The editor bridges live snapshots at bounded cadence, drops stale frames, survives close/reopen, and does not couple audio processing to WebView lifetime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render filled logarithmic spectrum in the existing stage</name>
  <files>ui/src/bridge/BridgeProvider.tsx, ui/src/components/AnalyzerStage.tsx, ui/src/components/SpectrumCanvas.tsx, ui/src/components/SpectrumCanvas.test.tsx</files>
  <behavior>
    - Test 1: `BridgeProvider` stores the newest valid snapshot without replacing ready/error host state.
    - Test 2: `AnalyzerStage` mounts `SpectrumCanvas` inside the existing `#spectrum-renderer-mount` region per D-11.
    - Test 3: Renderer creates one canvas and no per-bin DOM/MUI elements for a fixture containing many bins per D-10 and UI-02.
    - Test 4: Canvas draw path uses a smooth filled curve with dark-panel-compatible styling hooks per D-09.
  </behavior>
  <action>Create `SpectrumCanvas` using a `<canvas>` renderer with resize/device-pixel-ratio handling and imperative drawing from validated snapshot data. Replace the Phase 1 ready placeholder with live renderer behavior when a snapshot exists, while preserving connecting/error presentation. Use the existing dark LumaScope look with subtle glow/fill per D-09; do not add full analyzer controls, standalone source controls, or licensing UI.</action>
  <verify>
    <automated>npm.cmd --prefix ui run test:run; npm.cmd --prefix ui run build; npm.cmd --prefix ui run check:bundle</automated>
  </verify>
  <done>UI renders live snapshots through one bounded canvas renderer in the existing stage, passes protocol/renderer tests, and production bundle remains local/offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native snapshot -> WebView event | Native payload crosses into JavaScript and must be bounded/validated. |
| React bridge -> canvas renderer | Parsed snapshot values drive drawing and must not create unbounded DOM or memory work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | `parseSpectrumSnapshot` | mitigate | Reject wrong protocol version, bad profiles, non-finite numbers, oversized arrays, and malformed bins. |
| T-02-07 | Denial of Service | WebView renderer | mitigate | Use one canvas and bounded bin counts; do not create DOM/MUI nodes per FFT bin. |
| T-02-08 | Information Disclosure | Debug smoke hooks | mitigate | Preserve Phase 1 Debug-only diagnostic hooks and avoid adding Release-only test surfaces. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:

- `npm.cmd --prefix ui run test:run`
- `npm.cmd --prefix ui run build`
- `npm.cmd --prefix ui run check:bundle`
- `cmake --preset vs2019-debug`
- `cmake --build --preset vs2019-debug --target LumaScope_Standalone LumaScope_VST3 LumaScopeNativeTests --parallel 4`
- `ctest --preset vs2019-debug --output-on-failure`
</verification>

<success_criteria>
The VST3 can send bounded latest snapshots through protocol v1 and the React UI renders them smoothly in the existing analyzer stage without per-bin DOM nodes or audio-thread WebView work.
</success_criteria>

<output>
Create `.planning/phases/02-end-to-end-vst3-analyzer/02-03-SUMMARY.md` when done.
</output>
