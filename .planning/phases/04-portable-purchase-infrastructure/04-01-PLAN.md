---
phase: 04-portable-purchase-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/package.json
  - worker/package-lock.json
  - worker/tsconfig.json
  - worker/vitest.config.ts
  - worker/wrangler.toml
  - worker/src/index.ts
  - worker/src/routes.ts
  - worker/src/env.ts
  - worker/src/db/schema.ts
  - worker/src/db/migrations/0001_create_tables.sql
  - infra/manifest.yaml
  - infra/generated-state.json
  - infra/generated-state.schema.json
  - .gitignore
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - INFRA-01
  - CLOUD-03
user_setup: []
must_haves:
  truths:
    - "A developer can inspect a declarative YAML manifest that describes Worker, D1, secrets, environments, and Lemon Squeezy identifiers without any account credentials."
    - "A developer can find versioned D1 migration SQL files that define license, activation, webhook-idempotency, and audit tables."
    - "A developer can run `npm --prefix worker ci` and `npx wrangler deploy --dry-run` to verify the Worker skeleton compiles and configures."
    - "generated-state.json exists, is gitignored, and its schema documents what account-specific values the bootstrap script will populate."
    - "The requirements file is updated to reflect that INFRA-01 is in progress."
  artifacts:
    - path: "infra/manifest.yaml"
      provides: "Declarative logical infrastructure manifest without account secrets"
      contains: "environments"
    - path: "worker/wrangler.toml"
      provides: "Portable Wrangler configuration template with env variable placeholders"
      contains: "d1_databases"
    - path: "worker/src/db/migrations/0001_create_tables.sql"
      provides: "Versioned D1 migration creating license, activation, webhook-idempotency, audit tables"
      contains: "CREATE TABLE IF NOT EXISTS licenses"
    - path: "infra/generated-state.schema.json"
      provides: "Schema for the gitignored generated-state.json"
      contains: "account_id"
    - path: ".gitignore"
      provides: "generated-state.json and worker/node_modules are gitignored"
      contains: "generated-state"
  key_links:
    - from: "infra/manifest.yaml"
      to: "worker/wrangler.toml"
      via: "environment names, D1 binding names, Worker names"
      pattern: "lumascope-activation"
    - from: "worker/src/db/migrations/0001_create_tables.sql"
      to: "worker/src/db/schema.ts"
      via: "TypeScript type definitions mirror the SQL table schemas"
      pattern: "license"
    - from: "infra/manifest.yaml"
      to: "infra/generated-state.json"
      via: "bootstrap script reads manifest and writes generated state"
      pattern: "environments"
---

## Phase Goal

**As a** developer provisioning the LumaScope purchase infrastructure, **I want to** define all Worker, D1, and Lemon Squeezy resources in a portable manifest and versioned configuration, **so that** the same deployment can be reproduced in any Cloudflare account without committing account secrets.

<objective>
Create the definition layer for the LumaScope activation service: a declarative infrastructure manifest, portable Wrangler configuration, versioned D1 migration SQL, Worker skeleton with typed environment definitions, gitignored generated-state boundary, and updated requirement tracking.

Purpose: Establish the logical resource model and configuration contracts that all later infrastructure automation builds upon. No account credentials, resource UUIDs, or secrets are committed to version control.

Output: `infra/manifest.yaml`, `worker/wrangler.toml`, `worker/package.json`, Worker TypeScript skeleton, D1 migration SQL, `infra/generated-state.schema.json`, updated `.gitignore`, updated `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@C:/Users/Lawrence/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@README.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker configuration → deployment | wrangler.toml with environment variables must not leak account IDs or secrets |
| Repository → Cloudflare | Manifest defines logical resources; generated-state.json holds actual account identifiers |
| D1 schema → Worker query | SQL migration files define the contract; Worker code queries through typed bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | infra/manifest.yaml | mitigate | Manifest must contain zero account IDs, secrets, or resource UUIDs; only logical names |
| T-04-02 | Information Disclosure | infra/generated-state.json | mitigate | generated-state.json must be gitignored and documented as containing account-specific identifiers |
| T-04-03 | Tampering | worker/wrangler.toml | mitigate | wrangler.toml uses env variable references (`ENV_VAR`), never literal account IDs; validate on configure |
| T-04-04 | Information Disclosure | .gitignore | mitigate | .gitignore must explicitly exclude generated-state.json and worker/node_modules/ |
| T-04-SC | Tampering | npm packages | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |

No high-severity threat may remain unmitigated when this plan completes.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Worker npm packages before installing</name>
  <files>worker/package.json, worker/package-lock.json</files>
  <read_first>AGENTS.md (Context7 documentation rules), .planning/PROJECT.md (constraints section)</read_first>
  <action>Create `worker/package.json` with the following pinned dependencies:
    - `wrangler` pinned to `4.6.0` (or latest current v4 stable)
    - `vitest` pinned to `4.1.9` (matching existing ui/ version convention)
    - `typescript` pinned to `5.8.3` (or latest TS 5.8.x)
    - `@cloudflare/workers-types` pinned to current matching wrangler version
    - `@cloudflare/vitest-pool-workers` pinned for D1 integration tests
    - No `node:*` polyfill packages — Workers runtime provides Web Crypto natively
    Add scripts: `dev` (wrangler dev), `test` (vitest run), `test:watch` (vitest), `typecheck` (tsc --noEmit), `migrate:local` (wrangler d1 migrations apply --local), `migrate:remote` (wrangler d1 migrations apply). Do NOT install packages — only write package.json. Create `worker/package-lock.json` by running `npm.cmd --prefix worker install`. After install, verify each package on npmjs.com via websearch — check publisher reputation, age (>6 months), weekly downloads (>10k). If any package is [SUS], require human approval before proceeding.</action>
  <verify>
    <automated>npm.cmd --prefix worker ci; npm.cmd --prefix worker ls --depth=0 2>$null; Test-Path worker/package-lock.json</automated>
  </verify>
  <done>Worker package.json exists with pinned dependencies, npm ci succeeds, and all packages are verified as legitimate.</done>
  <what-built>Worker package.json with pinned Wrangler 4.x, Vitest, TypeScript, and @cloudflare Workers type definitions.</what-built>
  <how-to-verify>Run `npm.cmd --prefix worker ci` — must exit 0. Run `npm.cmd --prefix worker ls --depth=0` — verify wrangler, vitest, typescript, @cloudflare/workers-types are all present. Verify `worker/package-lock.json` exists. The executor should have validated each package on npmjs.com before install.</how-to-verify>
  <resume-signal>Type "approved" to confirm all packages are legitimate, or describe any concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create infrastructure manifest, generated-state boundary, Wrangler config, .gitignore, and Worker TypeScript skeleton</name>
  <files>infra/manifest.yaml, infra/generated-state.json, infra/generated-state.schema.json, worker/wrangler.toml, worker/tsconfig.json, worker/vitest.config.ts, worker/src/index.ts, worker/src/env.ts, worker/src/routes.ts, worker/src/db/schema.ts, .gitignore</files>
  <read_first>.planning/PROJECT.md (Constraints section — no secrets in Git, infrastructure portability), AGENTS.md (Technology Stack — Cloudflare Workers + D1, Wrangler v4), README.md (existing Cloud infrastructure references)</read_first>
  <action>Create the following files with exact specifications:

**1. `infra/manifest.yaml`**: Declarative logical infrastructure manifest version 1 with:
   - `name: lumascope-activation`
   - Three environments: `local`, `preview`, `production`
   - Each environment has: worker (name, route, d1_binding name, d1_database_name), secrets list (LEMON_WEBHOOK_SECRET, SIGNING_PRIVATE_KEY with required/optional flags and descriptions)
   - `lemon_squeezy` section: store_id (empty string — set after registration), webhook_secret_name, supported_events list (order_created, subscription_created, subscription_updated, subscription_cancelled, subscription_expired, license_key_created, license_key_updated)
   - `signing` section: public_key_secret_name, private_key_secret_name (both allocated for Phase 5, not yet used)
   - Zero account IDs, resource UUIDs, or secret values anywhere in the file

**2. `infra/generated-state.json`**: JSON file with empty placeholder fields:
   - `account_id`, `account_email`, `bootstrap_timestamp`, `wrangler_version`
   - `environments` object with `local`, `preview`, `production` each containing: `worker_id`, `worker_name`, `d1_database_id`, `d1_database_name`
   - All fields start as empty strings

**3. `infra/generated-state.schema.json`**: JSON Schema (draft-07) documenting all fields in generated-state.json with descriptions. Used by scripts to validate generated state before operations.

**4. `.gitignore`**: Append the following entries (do not remove existing entries):
   ```
   /infra/generated-state.json
   /worker/node_modules/
   /worker/dist/
   ```

**5. `worker/wrangler.toml`**: Portable Wrangler v4 configuration:
   - `name = "lumascope-activation"`, `main = "src/index.ts"`, `compatibility_date = "2026-04-01"`
   - Three environments (`env.local`, `env.preview`, `env.production`)
   - Each env has: `name`, `d1_databases` with binding `ACTIVATION_DB`, database_name matching manifest, database_id using descriptive placeholders (`"local"` for local env, `"PREVIEW_D1_ID"` for preview, `"PRODUCTION_D1_ID"` for production — these placeholders are replaced by bootstrap)
   - Route sections with placeholder comments
   - No account_id field — set via wrangler CLI or environment variable

**6. `worker/tsconfig.json`**: TypeScript config targeting `ES2022`, module `ES2022`, moduleResolution `bundler`, strict mode, skipLibCheck, include `src/**/*.ts`, types `["@cloudflare/workers-types"]`.

**7. `worker/vitest.config.ts`**: Vitest config for Worker tests:
   - Import `defineConfig` from `vitest/config`
   - Use `@cloudflare/vitest-pool-workers` pool for integration tests
   - Test root: `../tests/worker`
   - Environment options referencing `wrangler.toml` for D1 bindings

**8. `worker/src/env.ts`**: TypeScript `Env` interface with:
   - `ACTIVATION_DB: D1Database` (per wrangler binding)
   - `LEMON_WEBHOOK_SECRET: string` (from secrets)
   - `SIGNING_PRIVATE_KEY: string` (from secrets, placeholder for Phase 5)
   - `ENVIRONMENT: string` (from wrangler vars, values: `local`, `preview`, `production`)
   - Export the `Env` interface as default export

**9. `worker/src/routes.ts`**: Route definitions module:
   - Export a `handleRequest(request: Request, env: Env, ctx: ExecutionContext): Promise<Response>` function
   - Implement a URL-based router that dispatches to placeholder handlers
   - Register two routes: `POST /api/webhook/lemon-squeezy` → `handleLemonWebhook` (stub that returns 501 Not Implemented), `GET /api/health` → returns `{ status: "ok", version: 1, environment }` as JSON
   - Export individual handler function signatures so they can be implemented in Plan 04-03

**10. `worker/src/index.ts`**: Main Worker entry point:
   - Default export a `fetch` handler using `export default { async fetch(request, env, ctx) }` pattern
   - Set CORS headers for development (Access-Control-Allow-Origin, Methods, Headers)
   - Call `handleRequest` from routes.ts
   - Wrap in try/catch returning 500 with generic error envelope
   - Parse `env.ENVIRONMENT` to determine environment (default to "production" if unset)

**11. `worker/src/db/schema.ts`**: TypeScript type definitions for all D1 tables as interfaces:
   - `License` interface matching the SQL columns (id, lemon_order_id, license_key, product_id, variant_id, store_id, status, customer_name?, customer_email?, activated_count, created_at, updated_at)
   - `Activation` interface (id, license_id, machine_identifier, activated_at, last_validated_at, deactivated_at?, is_active)
   - `WebhookIdempotency` interface (id, event_id, event_type, event_created_at, processed_at, status, result_summary?)
   - `AuditLogEntry` interface (id, action, actor, resource_type, resource_id?, details?, ip_address?, timestamp, request_id?)
   - Export all interfaces. Export helper type `LicenseStatus = 'active' | 'expired' | 'refunded' | 'disabled' | 'pending'` and `IdempotencyStatus = 'completed' | 'failed' | 'skipped'`.

**Verification:** Run `npx wrangler deploy --dry-run` from the worker directory to validate wrangler.toml and TypeScript compilation. Write smoke verification JSON to document the dry-run result.</action>
  <verify>
    <automated>npm.cmd --prefix worker run typecheck; Push-Location worker; npx wrangler deploy --dry-run --env local 2>&1; Pop-Location</automated>
  </verify>
  <done>All definition-layer artifacts exist: manifest.yaml with zero secrets, gitignored generated-state.json with schema, portable wrangler.toml with env placeholders, Worker TypeScript skeleton that compiles and passes wrangler dry-run, D1 migration SQL with all four required tables, and typed schema interfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Create D1 migration SQL file and update REQUIREMENTS.md</name>
  <files>worker/src/db/migrations/0001_create_tables.sql, .planning/REQUIREMENTS.md</files>
  <read_first>worker/src/db/schema.ts, .planning/REQUIREMENTS.md (CLOUD-03 description)</read_first>
  <action>Create `worker/src/db/migrations/0001_create_tables.sql` with:
   - Header comment: `-- Migration 0001: Create license, activation, webhook-idempotency, and audit tables`
   - Two comment markers: `-- up: migration forward` and `-- down: migration rollback`
   - Under `-- up`: CREATE TABLE IF NOT EXISTS statements for four tables:
     * `licenses` with columns: id INTEGER PRIMARY KEY AUTOINCREMENT, lemon_order_id TEXT NOT NULL UNIQUE, license_key TEXT NOT NULL UNIQUE, product_id TEXT NOT NULL, variant_id TEXT NOT NULL, store_id TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active' with CHECK constraint for active/expired/refunded/disabled/pending, customer_name TEXT, customer_email TEXT, activated_count INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now'))
     * `activations` with columns: id INTEGER PRIMARY KEY AUTOINCREMENT, license_id INTEGER NOT NULL REFERENCES licenses(id) ON DELETE CASCADE, machine_identifier TEXT NOT NULL, activated_at TEXT NOT NULL DEFAULT (datetime('now')), last_validated_at TEXT NOT NULL DEFAULT (datetime('now')), deactivated_at TEXT, is_active INTEGER NOT NULL DEFAULT 1 CHECK(is_active IN (0,1)), UNIQUE(license_id, machine_identifier). Create index idx_activations_license_active ON activations(license_id, is_active) WHERE is_active = 1.
     * `webhook_idempotency` with columns: id INTEGER PRIMARY KEY AUTOINCREMENT, event_id TEXT NOT NULL UNIQUE, event_type TEXT NOT NULL, event_created_at TEXT NOT NULL, processed_at TEXT NOT NULL DEFAULT (datetime('now')), status TEXT NOT NULL DEFAULT 'completed' CHECK(status IN ('completed','failed','skipped')), result_summary TEXT. Create index idx_webhook_idempotency_event_id ON webhook_idempotency(event_id).
     * `audit_log` with columns: id INTEGER PRIMARY KEY AUTOINCREMENT, action TEXT NOT NULL, actor TEXT NOT NULL, resource_type TEXT NOT NULL, resource_id TEXT, details TEXT, ip_address TEXT, timestamp TEXT NOT NULL DEFAULT (datetime('now')), request_id TEXT. Create indexes on audit_log(timestamp) and audit_log(action).
   - Under `-- down`: DROP TABLE IF EXISTS statements in reverse dependency order (audit_log, webhook_idempotency, activations, licenses)

   Update `.planning/REQUIREMENTS.md`:
   - Change `CLOUD-03` status from `Pending` to `In Progress` (or add a Phase 4 tracking note)
   - Change `INFRA-01` status from `Pending` to `In Progress`
   - Add comment near Phase 4 requirements indicating Plan 04-01 coverage

   Ensure the SQL is valid D1/SQLite dialect. Each CREATE TABLE uses IF NOT EXISTS for idempotency. Column constraints and foreign keys follow SQLite syntax supported by D1.</action>
  <verify>
    <automated>npm.cmd --prefix worker run typecheck; Push-Location worker; npx wrangler d1 migrations list --env local 2>&1; Pop-Location</automated>
  </verify>
  <done>D1 migration SQL file exists with all four required tables, TypeScript type definitions mirror the schemas, and REQUIREMENTS.md is updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm.cmd --prefix worker ci` exits 0 and all pinned dependencies are installed.
- [ ] `npm.cmd --prefix worker run typecheck` exits 0 — TypeScript compiles without errors.
- [ ] `npx wrangler deploy --dry-run --env local` exits 0 from worker/ directory.
- [ ] `Test-Path infra/manifest.yaml` — YAML contains three environments and no account secrets.
- [ ] `Test-Path infra/generated-state.json` and `Test-Path infra/generated-state.schema.json`.
- [ ] `Get-Content .gitignore -Raw` matches `generated-state.json`, `worker/node_modules/`.
- [ ] `Get-Content worker/src/db/migrations/0001_create_tables.sql -Raw` contains all four CREATE TABLE statements.
- [ ] `.planning/REQUIREMENTS.md` has INFRA-01 and CLOUD-03 marked as in-progress.
</verification>

<success_criteria>
- All definition-layer artifacts exist in the repository: `infra/manifest.yaml`, `infra/generated-state.json`, `infra/generated-state.schema.json`, `worker/wrangler.toml`, `worker/package.json`, `worker/tsconfig.json`, `worker/src/index.ts`, `worker/src/env.ts`, `worker/src/routes.ts`, `worker/src/db/schema.ts`, `worker/src/db/migrations/0001_create_tables.sql`.
- No account IDs, resource UUIDs, or secret values are committed anywhere.
- The Worker skeleton compiles, runs wrangler dry-run, and the health endpoint stub returns JSON.
- `generated-state.json` is gitignored — a `git check-ignore infra/generated-state.json` exits 0.
- Four D1 migration tables (licenses, activations, webhook_idempotency, audit_log) are defined in both SQL and TypeScript.
- The infrastructure manifest describes three environments with logical resource names and zero secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-portable-purchase-infrastructure/04-01-SUMMARY.md`
</output>