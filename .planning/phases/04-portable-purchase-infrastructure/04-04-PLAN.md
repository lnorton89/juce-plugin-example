---
phase: 04-portable-purchase-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - docs/cloud-infrastructure.md
  - scripts/test-all.ps1
  - scripts/verify-project.ps1
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - INFRA-07
  - INFRA-08
  - CLOUD-01
  - CLOUD-02
  - CLOUD-03
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
user_setup: []
must_haves:
  truths:
    - "A fresh developer can follow docs/cloud-infrastructure.md to provision Cloudflare resources, deploy the Worker, and verify it works."
    - "The documentation explains the Lemon Squeezy webhook registration process, outputs the exact callback URL needed, and describes the secret exchange."
    - "test-all.ps1 runs Worker CI, typecheck, tests alongside all existing checks."
    - "verify-project.ps1 -SelfTest passes with all Phase 4 artifact checks."
    - "REQUIREMENTS.md updates all Phase 4 requirements to Completed."
    - "ROADMAP.md updates Phase 4 plan list to mark all 4 plans as complete."
  artifacts:
    - path: "docs/cloud-infrastructure.md"
      provides: "Complete cloud infrastructure documentation"
      contains: "bootstrap.ps1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All Phase 4 requirements marked complete"
      contains: "CLOUD-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 plans marked as complete"
      contains: "[x] 04-01"
  key_links:
    - from: "docs/cloud-infrastructure.md"
      to: "infra/bootstrap.ps1"
      via: "documentation references the bootstrap script"
      pattern: "bootstrap.ps1"
    - from: "docs/cloud-infrastructure.md"
      to: "worker/wrangler.toml"
      via: "documentation explains wrangler configuration"
      pattern: "wrangler.toml"
    - from: "docs/cloud-infrastructure.md"
      to: "infra/manifest.yaml"
      via: "documentation describes the logical manifest"
      pattern: "manifest.yaml"
---

## Phase Goal

**As a** developer completing the LumaScope cloud infrastructure setup, **I want to** follow comprehensive documentation to provision, deploy, and verify the Worker in my own Cloudflare account, register the Lemon Squeezy webhook, and confirm all Phase 4 requirements are met, **so that** the purchase infrastructure is fully documented, tested, and ready for Phase 5 activation service development.

<objective>
Complete Phase 4 by writing comprehensive cloud infrastructure documentation, running end-to-end verification with local Worker emulation, updating project verification and requirements tracking, and preparing the handoff to Phase 5.

Purpose: Ensure a fresh developer can follow repository documentation alone to provision Cloudflare resources, deploy the Worker, register the Lemon Squeezy webhook, and confirm the webhook processing pipeline works end-to-end.

Output: docs/cloud-infrastructure.md, updated README.md, updated .planning/REQUIREMENTS.md, updated .planning/ROADMAP.md, updated scripts/verify-project.ps1, updated scripts/test-all.ps1.
</objective>

<execution_context>
@C:/Users/Lawrence/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/Lawrence/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@infra/manifest.yaml
@infra/bootstrap.ps1
@infra/deploy.ps1
@infra/verify.ps1
@infra/teardown.ps1
@infra/common.ps1
@infra/generated-state.json
@infra/generated-state.schema.json
@worker/src/index.ts
@worker/src/webhook.ts
@worker/src/routes.ts
@worker/src/db/repository.ts
@worker/src/db/schema.ts
@worker/src/db/migrations/0001_create_tables.sql
@worker/wrangler.toml
@worker/package.json
@scripts/test-all.ps1
@scripts/verify-project.ps1
@README.md
@docs/development.md
@docs/troubleshooting.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation to developer | Documentation must not expose secrets, account IDs, or private keys |
| Repository to external systems | Webhook registration instructions guide the developer through Lemon Squeezy dashboard |
| Phase handoff | Phase 4 completion means all CLOUD-01/02/03 and INFRA-01-08 requirements are met |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | docs/cloud-infrastructure.md | mitigate | Documentation must use placeholder values (YOUR_ACCOUNT_ID, YOUR_SECRET) never real account identifiers |
| T-04-20 | Information Disclosure | .planning/REQUIREMENTS.md | mitigate | Requirements updates contain status changes only, no secrets |
| T-04-21 | Denial of Service | scripts/test-all.ps1 | mitigate | Worker test steps run before CMake steps; if Worker deps fail, the whole suite fails informatively |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages — all Worker dependencies already installed in Plan 04-01 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive cloud infrastructure documentation</name>
  <files>docs/cloud-infrastructure.md</files>
  <read_first>infra/manifest.yaml, infra/bootstrap.ps1, infra/deploy.ps1, infra/verify.ps1, infra/teardown.ps1, worker/wrangler.toml, worker/src/webhook.ts, docs/development.md (for documentation style patterns)</read_first>
  <action>Create docs/cloud-infrastructure.md with the following sections:

   # Cloud Infrastructure: LumaScope Activation Service

   ## Overview
   Explain that this is the cloud foundation for Lemon Squeezy purchase activation. Describe the architecture: Lemon Squeezy sends webhook events to a Cloudflare Worker, which verifies signatures, processes events idempotently, and stores license data in D1. Reference the relationship to Phase 5 (activation service) and Phase 6 (native licensing).

   The documentation MUST use placeholder values (YOUR_ACCOUNT_ID, YOUR_CLOUDFLARE_TOKEN) throughout — never use real identifiers.

   ## Architecture Diagram
   ASCII diagram showing:
   ```
   Lemon Squeezy --> Webhook (signed HMAC-SHA256) --> Cloudflare Worker
                                                        |
                                                    D1 Database
                                                    (licenses, activations,
                                                     idempotency, audit)
   ```

   ## Prerequisites
   - Cloudflare account (free tier sufficient for Workers + D1)
   - Node.js 22+ and npm
   - Git checkout of the repository
   - Lemon Squeezy store with a product configured

   ## Environment Variables
   Document these environment variables needed by bootstrap/deploy scripts:
   - CLOUDFLARE_API_TOKEN: least-privilege token with Workers:Edit and D1:Edit permissions
   - CLOUDFLARE_ACCOUNT_ID: 32-hex-char Cloudflare account identifier
   - LEMON_WEBHOOK_SECRET: HMAC-SHA256 signing secret from Lemon Squeezy webhook settings

   ## Infrastructure Manifest
   Explain infra/manifest.yaml — what it describes (Worker name, D1 databases per environment, secret requirements, supported Lemon events). Emphasize that it contains no secrets. Show example structure.

   ## Generated State
   Explain infra/generated-state.json — gitignored, holds account-specific IDs produced by bootstrap. Show the JSON schema reference.

   ## Step 1: Bootstrap Resources
   ```powershell
   powershell.exe -NoProfile -File infra/bootstrap.ps1 -Environment production
   ```
   Explain what this does: authenticates to Cloudflare, creates D1 databases, writes IDs to generated-state.json. Include the -Force flag for re-running.

   ## Step 2: Set Required Secrets
   ```powershell
   $env:LEMON_WEBHOOK_SECRET = "your-lemon-webhook-secret"
   powershell.exe -NoProfile -File infra/deploy.ps1 -Environment production
   ```
   Explain that secrets are read from environment variables only, never from files. List all required secrets per environment.

   ## Step 3: Deploy and Migrate
   Explain that deploy.ps1 applies D1 migrations, deploys the Worker, and injects secrets in one command. Show the output format and expected success message.

   ## Step 4: Verify Deployment
   ```powershell
   powershell.exe -NoProfile -File infra/verify.ps1 -Environment production -Verbose
   ```
   Explain what verify.ps1 checks. Show example [PASS]/[FAIL] output.

   ## Step 5: Register Lemon Squeezy Webhook
   This section fulfills INFRA-08:
   1. After deployment, get the Worker URL from deploy output or Cloudflare dashboard
   2. In Lemon Squeezy, go to Your Store > Settings > Webhooks
   3. Click "Add webhook"
   4. Enter the callback URL: https://lumascope-activation.YOUR-WORKER-SUBDOMAIN.workers.dev/api/webhook/lemon-squeezy
   5. Select events: order_created, subscription_created, subscription_updated, subscription_cancelled, subscription_expired, license_key_created, license_key_updated
   6. Copy the webhook signing secret and set it as LEMON_WEBHOOK_SECRET in your environment
   7. Re-run deploy.ps1 to inject the secret if it wasn't set before
   Output the exact callback URL pattern using placeholders so the developer can paste it.

   ## Teardown
   ```powershell
   powershell.exe -NoProfile -File infra/teardown.ps1 -Environment production -Force -DeleteD1
   ```
   Explain that teardown requires -Force or interactive confirmation. Show the resource list that gets printed before deletion.

   ## Development Workflow (Local)
   Explain how to use the local environment for testing without a real Cloudflare account:
   ```powershell
   powershell.exe -NoProfile -File infra/bootstrap.ps1 -Environment local
   npx wrangler dev --env local
   ```
   Then send test webhooks to http://localhost:8787/api/webhook/lemon-squeezy.

   ## Testing
   ```powershell
   npm.cmd --prefix worker run test
   ```
   Explain that Worker tests use mock D1 and no external infrastructure.

   ## Security Notes
   - Secrets are never committed to Git
   - Webhook signatures use constant-time HMAC-SHA256 verification
   - All D1 queries use parameterized prepared statements
   - Error responses are generic and do not leak internal state
   - Webhook events older than 5 minutes are rejected (replay protection)
   - Each event is processed at most once (idempotency via event_id)
   - Unconfigured store/product/variant events are silently skipped

   ## Next: Phase 5 — One-Machine Activation Service
   Brief description of what Phase 5 will build on top of this infrastructure.

   Follow the documentation styling of existing docs/development.md and docs/troubleshooting.md — clear markdown, PowerShell code blocks, consistent heading hierarchy.</action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "if (Test-Path docs/cloud-infrastructure.md) { Write-Host 'Documentation exists'; $content = Get-Content -Raw docs/cloud-infrastructure.md; if ($content -match 'bootstrap\.ps1' -and $content -match 'Lemon Squeezy' -and $content -match 'infra/verify\.ps1') { Write-Host 'Key sections present'; exit 0 } else { Write-Error 'Missing required sections'; exit 1 } } else { Write-Error 'File missing'; exit 1 }"</automated>
  </verify>
  <done>docs/cloud-infrastructure.md exists with all required sections: architecture overview, prerequisites, environment variables, step-by-step bootstrap/deploy/verify/teardown, webhook registration instructions with callback URL, local development workflow, testing, and security notes.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end verification with local Worker emulation and update project scaffolding</name>
  <files>scripts/test-all.ps1, scripts/verify-project.ps1, README.md, .planning/REQUIREMENTS.md</files>
  <read_first>scripts/test-all.ps1, scripts/verify-project.ps1, README.md, .planning/REQUIREMENTS.md</read_first>
  <action>Perform end-to-end verification steps:

   1. **test-all.ps1 update**: Ensure the Worker steps (npm ci, typecheck, test) are correctly positioned in the script flow. Run the full suite to confirm all existing Phase 1-3 checks pass alongside new Phase 4 Worker checks. Fix any issues.

   2. **verify-project.ps1 update**: Ensure all Phase 4 artifacts are in the $required list. Run verify-project.ps1 -SelfTest to confirm all five negative probes still pass (TutorialIdentity, ExtraContext7, RemoteAsset, PrivateKey, ReleaseDevServer). Add any missing Phase 4 assertions:
   - Require-Text for docs/cloud-infrastructure.md containing "bootstrap.ps1" and "Lemon Squeezy"
   - Require-Text for worker/webhook.ts containing "verifyLemonSignature" or "HMAC"
   - Require-Text for scripts/test-all.ps1 containing "worker run test"
   - Verify infra/generated-state.json is gitgnored (check .gitignore content)

   3. **End-to-end local emulation test**: Start npx wrangler dev --env local in the background, send a test POST request to the webhook endpoint with a known-good signature, capture the response, and confirm it returns 200 with status: "processed". This validates that:
   - The Worker compiles and starts
   - The routes dispatch correctly
   - The health endpoint responds
   - The webhook handler accepts a valid signature
   Use a PowerShell script that:
   - Starts wrangler dev as a background job
   - Waits for the local server to be ready
   - Sends a test webhook POST with computed HMAC signature
   - Verifies the response
   - Sends a health check GET and verifies the response
   - Stops the background job
   If npx wrangler dev cannot start in the test environment, document the limitation and verify via typecheck + test suite instead.

   4. **README.md update**: Add an "Activation Infrastructure" section after the existing "VST3 validation" section describing Phase 4 completion, pointing to docs/cloud-infrastructure.md for provisioning instructions. Update the phase table to mark Phase 4 as complete (using the same style: checkmark for complete).

   5. **REQUIREMENTS.md update**: Mark all Phase 4 requirements as Complete:
   - CLOUD-01: mark as [x] Complete
   - CLOUD-02: mark as [x] Complete
   - CLOUD-03: mark as [x] Complete
   - INFRA-01: mark as [x] Complete
   - INFRA-02: mark as [x] Complete
   - INFRA-03: mark as [x] Complete
   - INFRA-04: mark as [x] Complete
   - INFRA-05: mark as [x] Complete
   - INFRA-06: mark as [x] Complete
   - INFRA-07: mark as [x] Complete
   - INFRA-08: mark as [x] Complete
   Add a comment noting Phase 4 completion date.</action>
  <verify>
    <automated>powershell.exe -NoProfile -File scripts/test-all.ps1 2>&1; powershell.exe -NoProfile -File scripts/verify-project.ps1 -SelfTest 2>&1</automated>
  </verify>
  <done>test-all.ps1 runs Worker steps successfully. verify-project.ps1 -SelfTest passes all probes. README.md updated with Phase 4 completion. All Phase 4 requirements marked complete in REQUIREMENTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve Phase 4 completion — infrastructure, documentation, and handoff readiness</name>
  <files>docs/cloud-infrastructure.md, README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>docs/cloud-infrastructure.md, README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Pause execution after all automated checks pass and present the Phase 4 deliverables for human review. The user should verify:

   1. The cloud infrastructure documentation is complete, accurate, and can be followed by another developer
   2. The Lemon Squeezy webhook registration instructions are clear and include the callback URL pattern
   3. All scripts (bootstrap, deploy, verify, teardown) are correctly documented
   4. The README.md reflects Phase 4 completion and points to the new documentation
   5. REQUIREMENTS.md accurately marks all Phase 4 requirements as complete
   6. ROADMAP.md updates correctly

   If any issue is found, return to the relevant task for correction.</action>
  <verify>User returns "approved" or describes specific issues requiring correction.</verify>
  <done>User approval is recorded with no unresolved infrastructure, documentation, or workflow issue.</done>
  <what-built>Phase 4 cloud infrastructure: bootstrap/deploy/verify/teardown scripts, Worker webhook processing with Lemon Squeezy verification, D1 database schema, infrastructure manifest, and comprehensive documentation.</what-built>
  <how-to-verify>
    1. Review docs/cloud-infrastructure.md — does it cover prerequisites, bootstrap, deploy, verify, teardown, and webhook registration?
    2. Verify scripts/test-all.ps1 passes with Worker steps included.
    3. Verify scripts/verify-project.ps1 -SelfTest passes.
    4. Confirm REQUIREMENTS.md marks all 11 Phase 4 requirements as Complete.
    5. Confirm ROADMAP.md shows 4/4 plans completed.
    6. Type "approved" to confirm, or describe any specific issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues that need correction before Phase 4 can be finalized.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/cloud-infrastructure.md has all required sections (overview, prerequisites, steps 1-5, local dev, testing, security, next phase)
- [ ] Lemon Squeezy webhook registration section in docs includes exact callback URL format
- [ ] README.md has Activation Infrastructure section and updated phase table
- [ ] REQUIREMENTS.md marks all Phase 4 requirements as Complete
- [ ] ROADMAP.md shows all 4 Phase 4 plans completed
- [ ] test-all.ps1 runs Worker CI/typecheck/tests
- [ ] verify-project.ps1 -SelfTest passes all 5 negative probes
- [ ] User approves the Phase 4 handoff checkpoint
</verification>

<success_criteria>
- docs/cloud-infrastructure.md provides complete, accurate provisioning and deployment instructions that a fresh developer can follow.
- Lemon Squeezy webhook registration section outputs the exact callback URL and explains the secret exchange (INFRA-08).
- All infrastructure scripts (bootstrap, deploy, verify, teardown) are documented with exact commands and expected output.
- README.md reflects Phase 4 completion with a pointer to cloud-infrastructure.md.
- All Phase 4 requirements (CLOUD-01/02/03, INFRA-01 through INFRA-08) are marked Complete in REQUIREMENTS.md.
- ROADMAP.md marks 4/4 Phase 4 plans as complete.
- The project verification suite includes all Phase 4 artifact checks.
- Phase 4 is ready for Phase 5 (One-Machine Activation Service) development.
</success_criteria>

<output>
After completion, create `.planning/phases/04-portable-purchase-infrastructure/04-04-SUMMARY.md`
</output>
